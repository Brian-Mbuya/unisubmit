# UniSubmit · V4 research notes

Raw findings harvested from audit/research agents, appended lens-by-lens as each
completes. Written incrementally on purpose: an earlier run lost its synthesis to a
session limit, so nothing here waits on a final step.

Feeds `ROADMAP-V4.md` — this file is the evidence, the roadmap is the plan.

## Lens: first-run-adoption (recovered from failed run)

### First-boot admin password is unrecoverable, and the deploy docs contradict the code
- **severity** critical · **effort** M · **category** first-run-blocker
- **evidence** UnisubmitApplication.java:67-103; EmailService.java:44,52; AuthController.java:69-91; deploy/README.md:52-54; deploy/RAILWAY.md:17-28; deploy/unisubmit.env.example (no ADMIN_INITIAL_PASSWORD)
- **detail** On a fresh non-demo database `bootstrapAccounts` creates exactly ONE account (`admin`) whose password is either `ADMIN_INITIAL_PASSWORD` or a generated 20-char string logged ONCE at WARN (UnisubmitApplication.java:83-102). Three things then combine into a hard lockout:

1. The docs the operator actually reads are wrong or silent. `deploy/README.md:52-54` still says "The app auto-creates admin / lecturer / student (all password123) on first boot" — untrue since the demo gate landed (application.yml:111-114 defaults `SEED_DEMO_ACCOUNTS=false`). `deploy/RAILWAY.md:17-28` lists the required variables and does not mention `ADMIN_INITIAL_PASSWORD`. `deploy/unisubmit.env.example` — the file the server operator fills in — omits it entirely. Only `.env.example:23` (local dev) documents it.
2. There is no recovery. `AuthController.requestPasswordReset:78` routes any non-STUDENT to `passwordResetService.requestCode`, and `EmailService.sendPasswordResetCode` does `String to = user.getUsername()` (EmailService.java:44,52). The bootstrap admin's username is the literal string `admin`, not an email — the send fails, asynchronously, into a log. The UI still redirects to `/reset-password?sent`, telling the admin a code was emailed. The fallback branch (AuthController.java:83-89) only raises an in-app notification to ROLE_ADMIN users: "Reset it from Admin › Accounts" — circular when the admin is the locked-out party.
3. `ADMIN_INITIAL_PASSWORD` is ignored once an admin row exists (UnisubmitApplication.java:79-81), so setting it after the fact and redeploying does nothing.

An IT admin who deploys on Railway, closes the log tab, and comes back tomorrow has no path into their own installation short of manual SQL or wiping the database.
- **do** Take an operator-supplied email as the bootstrap admin's username (or store an email on the account) so the existing emailed-code reset actually works for them. Fail fast at boot with a clear message if a fresh DB has neither `ADMIN_INITIAL_PASSWORD` nor an existing admin, rather than generating a password nobody will read. Also write the generated password to a file under the upload dir, not only to stdout. Then fix `deploy/README.md:52-54`, add the variable to `deploy/RAILWAY.md` and `deploy/unisubmit.env.example`.
- **why** This is the very first interaction a school has with the product, and its failure mode is total and silent. Every other item in this audit is downstream of being able to log in at all.

### No guided setup exists — the admin lands on a dashboard of zeros with no next step
- **severity** critical · **effort** S · **category** onboarding
- **evidence** AdminDashboardController.java:45-70; admin/dashboard.html:21-52,66-91,152-203; admin/layout.html:36-94; grep setup|onboard|wizard across src/ finds no such controller/template; ROADMAP.md has no matching item
- **detail** There is no setup wizard, checklist, or first-run state anywhere: grepping `src/` for setup|onboard|wizard|getting.started|first.run|checklist returns only unrelated BrandingService and test hits. No controller or template covers it.

What the admin actually sees after logging in: `AdminDashboardController.dashboard` puts 6 population counters and 8 submission-status counters on the model, and `admin/dashboard.html` renders them as a stat strip of zeros (lines 21-52), a rail of zeros (96-119), and a Chart.js doughnut fed eight zeros (152-203) which draws an empty ring with no empty-state copy. The only forward guidance is a "Quick actions" grid (66-91) listing six destinations — faculties, departments, programmes, units, accounts, landscape — in no stated order, omitting Branding, Curricula, Lecturer mappings and Bulk import entirely, and including Research Landscape, which is meaningless until submissions exist.

The sidebar (admin/layout.html:36-94) groups 12 destinations under Overview / Academic structure / People / Intelligence. The grouping is sensible but conveys no sequence and no state — Curricula looks exactly as available on minute zero as it does after the tree is built, even though it cannot be used until programmes and units exist.

ROADMAP.md contains no onboarding or first-run item at all (grep for onboard|first-run|setup wizard|guided|adoption returns only demo-seeder lines 337-338 and 548). This is a blind spot, not deferred work.
- **do** Add a first-run checklist card to the dashboard driven entirely by counters already on the model — no new queries. Nine rows: Branding → Faculties → Departments → Lecturers → Programmes → Units → Curricula → Students → Enrolments/Teaching assignments, each linking to its screen, each ticking off when its counter goes non-zero, and each greyed with a "needs X first" note while its prerequisite is zero. Hide the card once all are satisfied. Replace the all-zero doughnut with an empty state pointing at the same checklist.
- **why** The admin knows their institution but not this app's data model. Nothing tells them that Faculty must precede Department must precede Programme, or that Curriculum is the join that makes submission possible. They will click around, create things in the wrong order, hit errors, and conclude the product is broken.

### The required import order is only half-documented, and the import page's own layout contradicts it
- **severity** high · **effort** S · **category** onboarding
- **evidence** admin/import.html:161,183,205,208-212,174-175,196-197; AcademicImportService.java:79-94,271-282; CsvImportService.java:164-167,435-438
- **detail** `admin/import.html` renders three blocks in DOM order: Students (161-180), Lecturers (183-202), Academic structure (205-231). That order is wrong.

Students carry `programmeCode`, validated against existing programmes (CsvImportService.java:164-167 → "Unknown programme code"). Lecturers carry `departmentCode`, validated against existing departments (CsvImportService.java:435-438). Both of those parents live in the Academic structure block *below* them on the page. In the other direction, `TEACHING_ASSIGNMENT` requires a `staffid` that resolves to a LecturerProfile and `ENROLLMENT` requires an `admissionnumber` that resolves to a StudentProfile (AcademicImportService.java:271-282) — so accounts must be created *before* those two.

The genuine order is interleaved and is written down nowhere: Faculties → Departments → **Lecturers** → Programmes → Units → Curriculum → **Students** → Teaching assignments → Enrolments.

What the UI offers instead: one sentence, "Import them in this order — each level references the one above it by code" (import.html:208-212), scoped only to the academic block, above seven visually identical unnumbered cards whose sequence is implied purely by the declaration order of the `Kind` enum (AcademicImportService.java:79-94). Nothing on the Students or Lecturers cards says what must exist first; their hints only say to use "YOUR programme codes (see Admin → Programmes)" (import.html:174-175) and "YOUR department codes" (196-197), which presumes those pages are already populated.
- **do** Number all nine uploads 1–9 in one continuous list, move the Students card between Curriculum and Teaching assignments and the Lecturers card between Departments and Programmes so DOM order equals required order, and label each card "Step N of 9 — requires: <parent kind>". Disable a card with an inline reason when its prerequisite count is still zero.
- **why** An admin working top-to-bottom down the page — the natural reading order — starts with the two imports that cannot possibly succeed yet. Their first two attempts fail with 100% invalid rows before they ever reach the block that explains ordering.

### Nine separate upload cycles, no progress memory, and the page wipes itself after every step
- **severity** high · **effort** M · **category** onboarding
- **evidence** AdminImportController.java:58-99,137-194; admin/import.html:161,183,205; admin/curricula.html:20-44; admin/assignments.html:20-51
- **detail** Making the app usable requires nine distinct bulk uploads (students, lecturers, and seven academic kinds), each a three-page-load loop: choose file → POST `.../preview` (re-renders `admin/import`) → POST `.../apply` → `redirect:/admin/import` (AdminImportController.java:58-99,137-194). That is ~27 page transitions, 9 template downloads, and 9 hand-authored spreadsheets — plus branding, which is a separate screen.

Worse, the page has no memory. All three upload blocks are wrapped in `th:if="${creds == null and preview == null and lecturerCreds == null and lecturerPreview == null and academicPreview == null}"` (import.html:161,183,205), so the instant any preview exists every other card vanishes — mid-flow you cannot see which steps remain. After apply you are redirected back to a flat list of nine identical cards with nothing marking what is already done. On step 7 of 9 the screen is indistinguishable from step 1.

The manual alternative is worse, not better: seven CRUD screens behind modals, where each curriculum row needs programme + unit + year + semester (admin/curricula.html:20-44) and each teaching assignment needs lecturer + curriculum + year + semester (admin/assignments.html:20-51). For a small faculty that is hundreds of modal submissions.
- **do** Two changes, both cheap. (a) Keep all nine cards rendered at all times with a per-kind status tick sourced from the counts the dashboard already queries, and show the preview inline under its own card instead of replacing the page. (b) Accept a single multi-sheet .xlsx whose sheet names map to `Kind` — `AcademicImportService.parseWorkbook` already opens a workbook and reads `wb.getSheetAt(0)` (line 215), so iterating sheets in enum order collapses nine uploads into one and makes the ordering problem disappear by construction.
- **why** Nine uploads with no progress indicator is where a busy IT admin abandons a pilot. The work is not intrinsically hard — the schema is small — but the interaction cost and the constant loss of place make it feel like it is.

### AcademicImportService.apply wraps the whole batch in one transaction while its javadoc and its UI both promise per-row safety
- **severity** high · **effort** S · **category** correctness
- **evidence** AcademicImportService.java:302-334 (@Transactional at 307, per-row catch at 325-329); AdminImportController.java:181-192; CsvImportService.java:42-43 (no @Transactional)
- **detail** `apply` is annotated `@Transactional` at the method level, covering the entire row loop (AcademicImportService.java:307-334). Its javadoc immediately above (302-306) states the opposite: "Applies row by row in its own transaction boundary per row via the repository calls — one bad row is reported and skipped rather than rolling back the batch, matching how the student importer behaves."

The per-row `catch (Exception ex)` at 325-329 increments `failed` and continues, but a persistence exception — a constraint violation, a flush failure — marks the surrounding transaction rollback-only. Commit then throws and *every* row created in that batch is discarded. Meanwhile `AdminImportController.applyAcademic` has already composed and flashed "N created, M already existed, 1 failed" from the returned `ApplyResult` (lines 181-192).

The comparison to the student importer is also wrong in the other direction: `CsvImportService` carries no `@Transactional` anywhere (class declared at line 42-43), so `applyStudents` genuinely is partial-safe per row via `userService.createStudent`'s own transaction. The two importers behave differently and the documentation asserts they behave the same.
- **do** Remove `@Transactional` from `apply` and put `@Transactional(propagation = REQUIRES_NEW)` on each applier, or wrap each row in a `TransactionTemplate` — the pattern `AIInsightProcessingService` already uses for exactly this reason. Then correct the javadoc. Add a test that asserts good rows survive a bad row in the same batch.
- **why** At adoption scale this is silent data loss with a success message on top: a 3,000-row enrolment import where one row trips a unique constraint reports "2,999 created" and persists nothing. The admin moves on believing the step is done, and the failure only surfaces weeks later when students cannot see their units.

### Wrong-order imports fail safely but never name the fix, and enrolment/assignment imports silently invent curriculum rows
- **severity** medium · **effort** S · **category** error-handling
- **evidence** AcademicImportService.java:258-285,288-298,376-393,396,411; admin/import.html:258-268,272; AdminImportController.java:152-160
- **detail** The good news first: ordering violations are caught before any write. `validate` pre-checks every parent reference (AcademicImportService.java:258-285) — programmes imported before departments produce `"Unknown department code 'CS'"` on every row, `validCount` is 0, and `admin/import.html:272` therefore never renders the Import button, only "Back". Nothing is written and the session stash is cleared on fatal previews (AdminImportController.java:152-160). That is well built.

Two gaps remain. First, the message stops at "Unknown department code 'CS'" and never says "import Departments first" — the admin is told what is missing but not that they are simply at the wrong step. And every failing row gets its own table row (import.html:258-268), so a 5,000-row file renders 5,000 identical red badges with no summary.

Second, and less visible: `applyCurriculum` is find-**or-create** (AcademicImportService.java:376-393), and both `applyTeachingAssignment` (396) and `applyEnrollment` (411) call it. Importing Enrolments before Curriculum therefore does not fail — it *fabricates* curriculum rows from the enrolment file's programme/unit/year/semester columns. No error, no warning, no mention in the preview. The Curricula screen later shows rows the admin never authored and cannot account for.
- **do** When every invalid row shares one parent-kind error, replace the row table with a single banner: "None of these rows can be imported yet — import Departments first" plus a direct link. Collapse repeated identical errors into a count. For the auto-create: either surface it in the preview ("this will also create 42 curriculum rows") or refuse it and require Curriculum to be imported first, consistent with every other kind.
- **why** The first failure is a wasted round trip that reads as a product defect rather than a sequencing mistake. The second is worse — it quietly diverges the admin's mental model of the curriculum from what is in the database, in exactly the table that governs which students can submit against which units.

### No in-app sample data, and the env-var path needs three flags plus a production security regression
- **severity** medium · **effort** M · **category** onboarding
- **evidence** CollaborationDemoSeeder.java:33-41,134-141,180,195,211; RichTestDataSeeder.java:22-24,87-114,117-140,143-180; application.yml:111-114,120-129; README.md:107-118 (documents the local profile only)
- **detail** There is no way to populate a demo from inside the app. Both seeders are `@ConditionalOnProperty`: `CollaborationDemoSeeder` on `unisubmit.demo.seed-collaboration` (line 39-40, @Order(10)) and `RichTestDataSeeder` on `unisubmit.demo.seed-rich-test-data` (line 22-24, @Order(15)), both defaulting false in production (application.yml:126,129). Changing either requires an environment variable and a restart.

They are also chained in a way no document records. `RichTestDataSeeder.seedRichTestData` aborts unless users `student` and `lecturer` exist (lines 109-114) and again unless the `DEMO-BCS`/`DCS410` curriculum created by `CollaborationDemoSeeder` exists (117-140, which logs "Seeding aborted" and returns). So `DEMO_SEED_RICH=true` on its own does nothing at all. It further mutates the seeded `S001`/`L001` profiles directly (143-180).

A populated evaluator demo therefore requires all three of `SEED_DEMO_ACCOUNTS=true`, `DEMO_SEED_COLLABORATION=true`, `DEMO_SEED_RICH=true` — and the first is the flag application.yml:111-114 explicitly warns against: "Enabling this in production recreates the old hole: a live admin login with a password that is in the source." So the only way to show a populated app is to reintroduce `admin`/`password123`.
- **do** Add an admin-only "Load sample data" action (and a matching "Remove sample data") that runs the seeders against synthetic accounts with generated passwords rather than the `password123` trio, decoupling demo content from `SEED_DEMO_ACCOUNTS`. Both seeders are already marker-guarded (CollaborationDemoSeeder:134-141, RichTestDataSeeder:89-95), so idempotence is solved. Until then, document the three-flag combination and its dependency order in deploy/RAILWAY.md.
- **why** An evaluator, a pilot department head, or the admin themselves cannot see what the product looks like when it is working. They see zeros and have to build a whole academic tree on faith before the first screen becomes meaningful — and the documented shortcut costs them a known-password admin account.

### Public self-registration is live from minute zero, yet impossible to complete until the tree exists — with no message either way
- **severity** medium · **effort** S · **category** first-run-blocker
- **evidence** SecurityConfig.java:44,50; register.html:47-66; AuthController.java:143-174 (params at 148-150, createStudent at 169)
- **detail** `SecurityConfig` permits `/register` anonymously (line 44) and `/api/academic/**` anonymously (line 50). Registration is therefore open to the internet from the moment the container starts, before the admin has configured anything: no invite, no email verification, no allowlist of valid admission numbers.

At the same time it cannot actually be used. `register.html` marks faculty, department and programme `required` (lines 47-66) and fills them by cascading fetches to `/api/academic/departments` and `/api/academic/programmes`. On a fresh deploy the faculty list is empty, so the first dropdown has nothing to select and the form cannot be submitted — with no explanation rendered. A student who finds the URL on day one sees three dead dropdowns.

The two facts do not cancel out, because the client-side `required` is the only gate: `AuthController.register` declares `courseId`, `departmentId` and `facultyId` as `required = false` (lines 148-150) and calls `createStudent(..., courseId, 1, 1)` at line 169. A direct POST creates a STUDENT with no programme.
- **do** Gate `/register` behind an admin toggle that stays off until at least one programme exists, and validate the submitted admission number against imported StudentProfiles so self-registration claims a pre-loaded roster row instead of minting arbitrary accounts. In the meantime, render "Registration isn't open yet — contact your department" when the faculty list comes back empty, and enforce the programme requirement server-side.
- **why** Two adoption problems in one endpoint: the school's deployment is publicly writable before the admin has done anything, and the legitimate first student is blocked by an unexplained empty form. Both land in the same week of a pilot.

### Bulk-imported students get a null semester, and a blank programme column silently creates a mis-attached cohort
- **severity** medium · **effort** S · **category** data-quality
- **evidence** CsvImportService.java:164-167,277-278,489; UserService.java:167-172; SubmissionService.java:93-110,130; StudentController.java:96-102; AuthController.java:169
- **detail** Two defaults in the student importer quietly degrade every account it creates.

Semester: the template is `name,phone,studentId,programmeCode,year` (CsvImportService.java:489) — there is no semester column — and `applyStudents` calls `createStudent(r.studentId(), r.phone(), password, r.name(), r.programmeId(), r.year(), null)` (lines 277-278), so `StudentProfile.currentSemester` is set to null (UserService.java:170-171). Submission still works, because curriculum resolution keys on programme alone (SubmissionService.java:93-99) and the unit dropdown is department-scoped (StudentController.java:96-102) — but CLASS_REP authority is scoped to programme+year+semester, and every semester-scoped view sees null for the entire imported population. Note the contrast: self-registration hardcodes year 1, semester 1 (AuthController.java:169), so the two entry paths produce differently shaped profiles.

Programme: `programmeCode` is optional — validated only when non-blank (CsvImportService.java:164-167). A school that omits the column imports cleanly and gets a whole cohort with no programme, which then falls into the lenient path in `SubmissionService:100-110`: "use the first curriculum for this unit". Every project from that cohort attaches to whatever curriculum happens to sort first, recorded only as an audit-log aside, "(no programme on file — first curriculum used)" (line 130).
- **do** Add `semester` to the student template and pass it through `applyStudents`. Keep `programmeCode` optional but make the preview warn on it: "N students have no programme — they will be able to submit, but their work will attach to an arbitrary curriculum." Reconcile the self-registration defaults (year 1 / semester 1) with the importer so both paths produce the same profile shape.
- **why** Both defects are invisible at import time — the preview shows green OK badges — and surface weeks later as submissions filed against the wrong curriculum and class reps whose scope does not match anyone. Re-importing does not fix them, because admission numbers already exist.

### Branding — the admin's step zero — cannot be saved if the in-page theme engine fails
- **severity** low · **effort** S · **category** robustness
- **evidence** AdminBrandingController.java:37-40,60-62,65-71; admin/branding.html:756-761; static/js/vendor/theme-engine.iife.js
- **detail** `AdminBrandingController.saveBranding` rejects an empty `tokensJson` with "No theme was generated in the browser — reload the page and try again" (lines 65-71), and the page refuses to post without generated tokens, showing "No theme generated yet — adjust a color or upload a logo first" (admin/branding.html:756-761). The entire palette is computed client-side by `static/js/vendor/theme-engine.iife.js`.

The engine is self-hosted so there is no CDN dependency, and the error message is honest rather than the opaque Jackson end-of-input it replaced. But the server already receives `basePrimary`, `baseSecondary` and `baseNeutral` as separate parameters (lines 60-62, defaulted for the pickers at 37-40) and does nothing with them when tokens are missing. The failure mode is a reload loop with no fallback, on the screen that is step one of adoption and the one most likely to be attempted on a low-end phone or a locked-down campus browser.
- **do** When `tokensJson` is absent, derive a minimal token block server-side from the three base colours already posted, save it, and flash "Saved with a basic palette — reopen Branding on a desktop browser for the full theme." Keep the current message only when the base colours are missing too.
- **why** Branding is the first thing an adopting school does and the change that makes the deployment feel like theirs. A dead end here — on any device where the engine does not run — reads as the product being broken before any real data has been entered.


## Lens: competitive-edtech + cross-industry-ux

### Deadlines are announcements, not a coursework object — no due/cut-off split, no penalty maths, no per-student extension
- **severity** critical · **effort** L · **category** deadlines-and-late-policy
- **evidence** https://docs.moodle.org/29/en/Assignment_settings ; https://www.monash.edu/learning-teaching/teachhq/moodle/assignment/how-to/extensions-and-resubmissions ; https://www.kenyon.edu/offices-and-services/center-for-innovative-pedagogy/instructional-technology/moodle/moodle-assignments-due-dates-and-cutoff-dates/ ; https://city-uk-ett.libguides.com/staff/moodle/extensions-resubmissions/Moodle-Assignment
- **detail** UniSubmit has no Assignment entity. A deadline exists only as `Announcement.deadline` plus a `lateWindowOpen` boolean (A:/unisubmit-main/unisubmit-main/src/main/java/com/unisubmit/domain/Announcement.java:38-42, toggled via LecturerController.toggleLateWindow at line 222), or loosely as `Unit.submissionDeadline` (AdminUnitController.java:57-64). `SubmissionVersion.late` is a bare boolean with no minutes-late value. The actual penalty rule is prose typed into an announcement message — literally '10% penalty' as free text in RichTestDataSeeder.java:264. Nothing computes it, nothing enforces it, and there is no way to give one student an extension without opening the late window for the entire class.

Every mature platform separates two distinct instants. Moodle's due date is when penalties begin but submission is still possible; the cut-off date is when the submit button disappears entirely; and extensions are granted per student via overrides, individually or by ticking a set of students in the grading table. Canvas does the same through differentiated 'assign to' dates. The two-date model is what makes 'late' a computable quantity instead of a flag.
- **do** Add an `Assignment` entity in com.unisubmit.domain keyed to `Curriculum` (which already carries programme+unit+year+semester): title, instructions, openAt, dueAt, cutoffAt, latePolicy enum (NONE | FLAT | PER_DAY), penaltyPercent, penaltyCapPercent, maxAttempts, and a rubric FK (see finding 2). Add `Submission.assignment` (nullable, so existing free-standing project submissions keep working) next to the existing `curriculum` FK in Submission.java. Add `AssignmentExtension` (assignment, student, newDueAt, newCutoffAt, reason, grantedBy, grantedAt) so a lecturer overrides one student without touching the class. Replace the `lateWindowOpen` boolean and the toggle-late-window endpoint entirely — cutoffAt subsumes it; keep Announcement for actual announcements. Change `SubmissionVersion.late` to `minutesLate` (Integer, null = on time) and store rawScore / penaltyApplied / finalScore on the grade record so the arithmetic is auditable rather than mental. Because CLASS_REP is already a scoped authority, let a class rep *request* a class-wide extension that the lecturer approves in one click — that maps directly onto how Kenyan cohorts actually negotiate deadlines.
- **why** Without a due/cut-off split the lecturer's only lever is a class-wide on/off switch, so one student's hospital admission forces the deadline open for 300 people. Late penalties applied by hand in a spreadsheet are the single largest source of grade disputes, and an uncomputed penalty is an unappealable one.

### Grading is one free-text blob plus a 0–100 integer — no rubric, so marks are neither consistent nor explainable
- **severity** high · **effort** L · **category** grading-workflow
- **evidence** https://guides.gradescope.com/hc/en-us/articles/22249389005709-Grading-submissions-with-rubrics ; https://people.eecs.berkeley.edu/~pabbeel/papers/2017-LaS-gradescope.pdf ; https://www.elearning.fse.manchester.ac.uk/getting-started-with-canvas/canvas-essentials-rubrics/ ; https://support.google.com/edu/classroom/answer/9093530
- **detail** `Feedback` (A:/unisubmit-main/unisubmit-main/src/main/java/com/unisubmit/domain/Feedback.java) is a TEXT message, a lecturer FK, and `Integer grade` (0–100, nullable). The lecturer UI in A:/unisubmit-main/unisubmit-main/src/main/resources/templates/lecturer/review-split.html:135-149 is a textarea and a number input. That is the entire grading model. The student is told '68' and a paragraph, and has no way to see where the other 32 marks went.

Gradescope's core insight is that a rubric is not a form you fill in before grading — it is a structure you *build while grading*: the rubric starts with one item, a grader adds an item when they meet a new mistake, and changing an item's point value retroactively rescores every submission already graded. Canvas surfaces the rubric to students before they submit, which converts it from a marking tool into an expectations tool. Google Classroom pitches rubrics primarily as transparency: students know what is required before submitting.
- **do** Add `Rubric` (owned by Assignment), `RubricCriterion` (label, description, maxPoints, ordinal), and optionally `RubricLevel` for banded descriptors. Add `RubricScore` (feedback FK, criterion FK, pointsAwarded, comment) and derive `Feedback.grade` as the sum instead of typing it — keep the existing integer column as the computed total so LecturerController.exportMarks and every existing query keep working unchanged. In review-split.html replace the single grade input with a criterion list (each row: descriptor + points input + optional per-criterion note), keeping the free-text box as an 'overall comment'. Critically, copy Gradescope's retroactive behaviour: editing a criterion's maxPoints or a shared deduction recomputes every RubricScore already recorded for that assignment — a single UPDATE plus a recompute of the parent Feedback totals. Render the same rubric read-only on student/new-submission.html *before* upload and on student/submission-detail.html after grading, so the marking scheme is the brief.
- **why** A bare number is the least actionable feedback possible, and it is the form most student-built apps stop at. Rubrics simultaneously fix three problems the roadmap treats separately: grader consistency, student clarity about what feedback means, and appeal-ability. It also unlocks per-criterion analytics for free (which criterion did the cohort fail?).

### No anchored feedback — comments float beside the document instead of pointing at the sentence they are about
- **severity** medium · **effort** L · **category** inline-annotation
- **evidence** https://community.canvaslms.com/t5/Instructor-Guide/How-do-I-add-annotated-comments-in-student-submissions-using/ta-p/694 ; https://docs.github.com/en/education/manage-coursework-with-github-classroom/teach-with-github-classroom/leave-feedback-with-pull-requests ; https://td.usnh.edu/TDClient/60/Portal/KB/ArticleDet?ID=1392
- **detail** Feedback attaches to a whole `SubmissionVersion`. A lecturer writing 'your methodology section confuses validity and reliability' has no way to point at the paragraph. Canvas DocViewer lets graders drop point, highlight, strikeout, free-text and area annotations directly onto PDF/DOC/PPT submissions inside SpeedGrader. GitHub Classroom achieves the same thing without a document viewer at all: it opens a permanent 'Feedback' pull request per student repo, and the instructor leaves line-anchored review comments on the diff.

UniSubmit already has the hard half of this built. Tika extraction means the plain text of every version is already in hand, and GROBID/OCR services exist alongside it.
- **do** Do NOT build a PDF canvas overlay — it is a JS project and breaks the minimal-JS, 512MB constraint. Build the GitHub Classroom variant instead: a server-rendered read view of the Tika-extracted text, paginated by paragraph with a stable index, and an `InlineComment` entity (submissionVersion FK, paragraphIndex, charStart, charEnd, quotedText snapshot, body, author, resolved flag, optional rubricCriterion FK). Render it as a two-column reading pane in lecturer/review-split.html — text left, comment thread right, anchored rows aligned by paragraph. Store `quotedText` denormalised so a re-extraction or a new version never orphans a comment. Students see the identical view on student/submission-detail.html, and a comment tied to a rubric criterion answers 'why did I lose these marks' at the exact sentence. This extends `Feedback` rather than replacing it: overall comment stays, anchored comments are the new child collection.
- **why** Unanchored feedback is the most common complaint about student-built submission tools — the student reads 'unclear in places' and cannot find the places. Anchoring is what turns feedback into revision instructions, and it composes with the existing versioning: a resolved comment on v1 that reappears on v2 is a visible signal of non-engagement.

### No comment bank — lecturers retype the same twenty sentences across a 200-student cohort
- **severity** high · **effort** S · **category** bulk-operations
- **evidence** https://teaching.pitt.edu/resources/speedgrader-comment-library/ ; https://ready.msudenver.edu/canvas-spotlight/using-speedgrader-comment-library/ ; https://shakeuplearning.com/blog/google-classroom-comments-all-you-need-to-know/
- **detail** Feedback is typed fresh into the textarea at review-split.html:139 every single time. Across a cohort, 80% of what a lecturer writes is a repeat: 'cite a primary source here', 'your literature review has no critical synthesis', 'the ER diagram is missing cardinality'. Canvas ships a SpeedGrader Comment Library where frequently used comments are saved and inserted into the comment box for multiple students and assignments. Google Classroom ships the same thing as a comment bank, explicitly framed as: if you enter the same comment for many students, save it to use later.
- **do** Add `FeedbackSnippet` (owner FK, optional unit FK, optional rubricCriterion FK, shortLabel, body, useCount, lastUsedAt). Render it as a collapsible panel beside the feedback textarea in lecturer/review-split.html — click inserts at cursor, a small 'save this comment' link banks whatever is currently typed. Order by useCount so the top five surface without searching. Two cheap multipliers: (a) allow department-level shared snippets so a course team converges on shared language, seeded from the existing admin screens; (b) attach a snippet to a rubric criterion so selecting a criterion band offers its usual comments. This is roughly one entity, one repository, one fragment in fragments/components.html, and about thirty lines of JS — the highest time-saved-per-line-of-code item on this list.
- **why** This is the single cheapest lecturer-time win available and it is what a demo audience of actual lecturers will notice first. It also improves feedback quality, because a banked sentence is one the lecturer wrote carefully once rather than badly at 1am on the two-hundredth script.

### Grades go live the instant they are saved — no marking state, no held release, no bulk publish
- **severity** high · **effort** M · **category** grading-workflow
- **evidence** https://help.lafayette.edu/using-grading-workflow-in-moodle/ ; https://www.gla.ac.uk/myglasgow/moodle/guides/assignmentmarkingworkflow/ ; https://blogs.chapman.edu/academics/2025/05/19/manual-posting-policies-vs-hiding-grades-in-canvas/ ; https://support.canvas.fsu.edu/kb/article/1386-how-do-i-use-a-grade-posting-policy-in-my-gradebook/
- **detail** `SubmissionService.addFeedbackAndReview` writes the Feedback and flips SubmissionStatus in one transaction (LecturerController.java:204-211), so a half-marked cohort is visible to students immediately and inconsistently. The lecturer cannot mark 50 scripts, notice the rubric was too harsh on Q3, and fix it before anyone sees a number.

Moodle models this as an explicit grading workflow: each student's grade moves through Not graded → In grading → Grading completed → In review → Ready for release → Released, students always see the *state* but only see grades and feedback once the state is Released, and the state can be set for many students at once from the 'With selected…' bulk action. Canvas expresses the same idea as a per-assignment grade posting policy — Automatic (visible on entry) or Manual (held until the instructor clicks Post Grades) — and is careful to distinguish a manual posting policy from after-the-fact hiding, because hiding still fires notifications.
- **do** Add `gradingState` to the grade record (NOT_GRADED, IN_GRADING, MARKING_COMPLETE, IN_REVIEW, READY_FOR_RELEASE, RELEASED) and `postingPolicy` (AUTOMATIC | MANUAL) to the new Assignment entity. Gate all student-facing grade and feedback rendering in student/submission-detail.html and student/dashboard.html on RELEASED, and gate the notification fired by NotificationService on the release transition rather than on save — that is the trap Canvas warns about. Add a 'Release all marks for this assignment' bulk action on lecturer/dashboard.html. Show students the state itself, not a blank: 'Marking in progress' is a far better empty state than nothing, and it directly serves the 17-templates-without-empty-states item already on the roadmap. Also expose an internal-only note field separate from the student-visible comment — a moderator's 'check this one, possible collusion' must never leak on release.
- **why** Publishing partial results is how a grading system loses staff trust in week one — students compare marks on WhatsApp within minutes and a mid-cohort rubric correction becomes a public retraction. Held release also makes moderation and second-marking possible at all, which most institutional policies require for final-year projects.

### Similarity is built for collaborator discovery only — there is no originality view, and semantic vectors alone will accuse the innocent
- **severity** high · **effort** L · **category** originality-reporting
- **evidence** https://guides.turnitin.com/hc/en-us/articles/34400565079053-Turnitin-and-plagiarism ; https://guides.turnitin.com/hc/en-us/articles/23435833938701-Understanding-the-similarity-score ; https://guides.turnitin.com/hc/en-us/articles/23539146689549-How-exclusion-filters-refine-the-Similarity-Report ; https://guides.turnitin.com/hc/en-us/articles/28477544839821-Turnitin-s-AI-writing-detection-capabilities-FAQs ; https://www.buffalo.edu/lms/guides-instructors/integrations/turnitin.html
- **detail** `SubmissionSimilarity` already stores a composite score with a per-signal breakdown (keywordScore, titleScore, unitScore, matched technologies/research areas) — genuinely good infrastructure. But it is surfaced exclusively as `similarProjectsPanel` on student/submission-detail.html:88 in a 'find a collaborator' framing, and EvaluationService measures it against accepted collaboration requests as ground truth. There is no staff-facing integrity view anywhere.

Two things follow from how Turnitin does this. First, the framing: Turnitin is explicit that it does not check for plagiarism — it highlights matches for a human to review, a high score does not imply plagiarism and a low score does not imply its absence, and the report is not a replacement for instructor expertise. Second, the mechanics: usable reports depend on exclusion filters — quoted material, bibliography (detected automatically), and matches under N words — because without them every properly-cited paper looks guilty. Turnitin also treats student visibility as an instructor-controlled setting, and deliberately withholds the AI-writing indicator from students entirely.

The specific technical risk for UniSubmit: pgvector cosine similarity is a *topical* measure. Twenty students told to build a school fee management system in PHP will all score 0.9 against each other while sharing not one sentence. A semantic score presented as an originality signal is a false-accusation engine.
- **do** Split the concept in two rather than reusing one score. Keep the existing panel as 'Related work' for discovery. Add a separate staff-only Originality panel on lecturer/review-split.html driven by a *verbatim* layer: hash overlapping word-shingles (k≈8) of the Tika-extracted text into a `TextFingerprint` table, and report longest common passages with the matched text shown side by side — that is cheap, deterministic, needs no API key, and is what actually evidences copying. Report the two numbers separately and label them honestly: 'Topic overlap 91% (same subject area — expected)' vs 'Verbatim overlap 34% across 7 passages'. Add exclusion toggles for quoted text, everything after a Bibliography/References heading, and matches under N words, and recompute the percentage live. Put a fixed interpretive line under every report: matches are not proof of plagiarism and require review. Gate student visibility behind a per-assignment `showOriginalityToStudent` flag, defaulting off, and — since Kenyan universities almost always require it — make the self-check case explicit: let students see their own verbatim report on a DRAFT before the deadline so they can fix citation errors, which is the use that actually improves work.
- **why** An originality report is the feature that makes staff take a submission system seriously, and it is the feature a student-built app most often gets dangerously wrong by shipping a raw similarity percentage with no exclusions and no framing. Given the pgvector engine already installed, the risk is not that this is missing — it is that the existing score gets repurposed as an accusation.

### No submission receipt — students have no portable proof they submitted, and no email address to send one to
- **severity** high · **effort** S · **category** student-clarity
- **evidence** https://libguides.dundee.ac.uk/turnitin-student/download-receipt ; https://helpguides.webspace.durham.ac.uk/software/turnitin/turnitin-student-guides/submission-receipts/ ; https://ucf.my.site.com/ucfinfo/s/article/Download-Receipt-and-Proof-of-Submission-Turnitin-1648828642827 ; https://sites.reading.ac.uk/tel-support-students/turnitin-finding-your-submission-id/
- **detail** Upload writes a SubmissionVersion and the student sees a success banner (`submissionReceived` in student/dashboard.html:26). That banner is gone on the next page load. There is no receipt, no reference number, nothing a student can produce three weeks later when the record is disputed.

Turnitin treats this as load-bearing infrastructure: every submission produces a digital receipt carrying a unique paper/submission ID and the exact date and time, downloadable as a PDF, emailed automatically, and universities state flatly that the digital receipt is the only acceptable proof of submission. Institutions instruct students not to consider themselves finished until they have saved it.

This matters more for UniSubmit than for any of the platforms studied, because students log in with admission number + phone and have no student email — so the usual 'check your inbox for confirmation' fallback does not exist. The evidence has to live in the app.
- **do** On every SubmissionVersion, generate an immutable receipt: a short human-quotable code (e.g. UNI-7K3M-2Q9X), the server timestamp, the assignment, the filename, byte size, the SHA-256 `contentHash` already computed in SubmissionVersion.java:47, and the on-time/late verdict with minutes-late from finding 1. Add a permanent `/submission/{id}/version/{n}/receipt` page — plain, print-stylesheet-friendly, no auth-gated assets — and link it from the version row on student/submission-detail.html rather than only from the transient banner. Show the content hash to the student as 'document fingerprint'; it is the answer to 'the file you have is not the file I uploaded'. Two adaptations for this deployment: cache the receipt page in the existing service worker so it survives offline, and make the receipt code the reference a student quotes over the phone or in a WhatsApp message to their class rep, since there is no email round-trip available. Lecturers and class reps should be able to look a submission up by receipt code.
- **why** 'I submitted it and the system lost it' is the highest-stakes dispute in coursework, and with no student email address UniSubmit currently has no artefact to settle it. This is a small amount of code that converts the app from something a student trusts to something a registrar trusts, and it demos in ten seconds.

### No structured regrade route — appeals happen off-platform and leave no audit trail
- **severity** medium · **effort** M · **category** grading-workflow
- **evidence** https://guides.gradescope.com/hc/en-us/articles/22237994239885-Managing-Regrade-Requests ; https://guides.gradescope.com/hc/en-us/articles/21854736042253-Submitting-a-Regrade-Request ; https://sites.reading.ac.uk/TEL-Support-Staff/gradescope-disable-regrade-requests/
- **detail** Once a grade is saved there is no student-initiated path back. A student who thinks a mark is wrong has to find the lecturer in person or on WhatsApp, and the resulting adjustment lands in the database with no record of who asked, why, or what changed.

Gradescope makes regrade requests a first-class object: they are enabled by default on published assignments, a student attaches a short statement to a *specific question*, the instructor sees a notification on that question, replies and clicks Close & Respond, and the student is notified and can see the status of every request they have made in one place. Instructors control it per assignment — enable/disable plus a start and end date for the request window. Two details are worth copying exactly: the responder's identity is hidden from the student, and the instructor chooses between editing the shared rubric item (which rescores everyone) and applying a point adjustment to this one student.
- **do** Add `RegradeRequest` (submission FK, optional rubricCriterion FK, studentStatement, state OPEN | RESOLVED | DECLINED, responderComment, resolvedBy, openedAt, resolvedAt, scoreBefore, scoreAfter). Open the window only after the grade reaches RELEASED (finding 5), and bound it with regradeOpensAt / regradeClosesAt on Assignment so appeals do not arrive six months later. Anchor requests to a rubric criterion — 'I think criterion 3 was misread' is a reviewable claim in a way that 'my mark is unfair' is not. Surface an open-requests count on lecturer/dashboard.html, resolve inline in review-split.html, and give students a status list on student/dashboard.html. Keep the responder anonymous to the student as Gradescope does. Every resolution should write to the existing AuditLog/AuditService with before/after scores, which turns an argument into a record. When the fix is a rubric change rather than a one-off, reuse the retroactive rescore from finding 2 and notify everyone affected.
- **why** Appeals happen whether or not the system supports them; the only question is whether they are logged. A structured queue caps the WhatsApp negotiation, gives the lecturer one place to work through, and produces exactly the audit trail an examinations board asks for. It also pairs naturally with the CLASS_REP role, which is already the informal channel for this.

### Bulk operations stop at one CSV export — no batch download, no offline marking round-trip, no non-submitter list
- **severity** high · **effort** M · **category** bulk-operations
- **evidence** https://digi-ed.uk/support/article/marking-offline-in-moodle-using-bulk-download-and-upload-2/ ; https://docs.moodle.org/502/en/Using_Assignment ; https://guides.gradescope.com/hc/en-us/articles/22239057709453-Exporting-Grades-Evaluations-and-Submissions ; https://help.ohio.edu/TDClient/30/Portal/KB/ArticleDet?ID=1078 ; https://help.lafayette.edu/using-grading-workflow-in-moodle/
- **detail** `LecturerController.exportMarks` (line 161) exports one CSV per unit: Student ID, Name, Title, Status, Grade, Graded by, Graded on. That is the only bulk operation in the app, and it is one-way. Everything else — opening a submission, downloading a file, writing feedback, setting a status — is one student at a time through the web UI.

The platforms converge on three bulk primitives. Moodle offers 'Download all submissions' as a ZIP plus a 'Download grading worksheet' CSV, and — the important half — 'Upload multiple feedback files in a zip' and re-upload of the completed worksheet, matched back to students by the original filename structure, which is why the guides insist you must not rename the files. Canvas exposes download-all-submissions from the assignment page. Gradescope exports grades as CSV or Excel from Review Grades, plus per-question 'Export Evaluations' spreadsheets and per-question/per-rubric statistics. Moodle's grading table also does bulk 'With selected…' actions for workflow state and marker allocation.
- **do** Extend rather than replace exportMarks. (1) 'Download all submissions' as a streamed ZIP per assignment, filenames as `{admissionNumber}_{surname}_{submissionId}_v{n}.{ext}` — stream via StreamingResponseBody straight from FileStorageService so a 300-file ZIP never materialises in the 512MB heap. (2) 'Download grading worksheet': a CSV of submissionId, admission number, name, one column per rubric criterion, plus comment and status — and accept it back on upload, matching on submissionId, validating each row, and showing a diff-style confirmation before commit. That round-trip is disproportionately valuable in a Kenyan deployment: a lecturer downloads once on campus wifi, marks a whole cohort offline at home, uploads once. (3) A checkbox column on lecturer/dashboard.html with bulk actions: release marks, set status, assign second marker, apply a banked comment (finding 4) to the selection. (4) A 'Not yet submitted' tab computed from Enrollment minus submissions for the assignment, with a one-click nudge through the existing NotificationService and the class rep. (5) Widen exportMarks to per-criterion columns keyed on admission number so it drops straight into the registrar's spreadsheet.
- **why** This is where a lecturer's evening actually goes, and it is what makes a demo land with the department rather than just the examiner. The offline worksheet round-trip in particular is a bandwidth-adapted feature that the international platforms treat as legacy but that fits this deployment's constraints better than it fits theirs.

### The student dashboard shows status badges, not a prioritised 'what is due, what changed, what to do next' feed
- **severity** high · **effort** M · **category** student-clarity
- **evidence** https://support.google.com/edu/classroom/answer/9093530 ; https://it.umn.edu/services-technologies/how-tos/canvas-students-view-feedback ; https://www.elearning.fse.manchester.ac.uk/getting-started-with-canvas/canvas-essentials-rubrics/ ; https://askathena.brynmawr.edu/help/moodle-grading-workflow
- **detail** student/dashboard.html partitions submissions into approved / needs-changes / active and renders a status badge per row (lines 4-6, 98). It answers 'what is the state of my things' but not the three questions a student actually opens the app with: what is due soonest, what changed since I last looked, and what am I supposed to do about it. REJECTED renders as a badge reading 'REJECTED' — a verdict, not an instruction — and an AI insight state is shown with equal weight to a grade.

Google Classroom's whole design centre is a to-do list ordered by due date; private comments are attached to returned work so the student meets the comment in the context of the thing it is about. Canvas gives students a recent-feedback surface and lets them read the rubric before submitting. Moodle deliberately shows a grading *state* even while the grade is withheld, so 'nothing yet' is never ambiguous.
- **do** Restructure student/dashboard.html around two stacked sections above the existing list. 'Due next': assignments from finding 1 sorted by effective due date (extension-aware), each showing time remaining, whether a draft exists, the late penalty that will apply if submitted now ('submit today: −10%'), and one primary verb — Submit / Resubmit / View feedback. 'What changed': a reverse-chronological feed built from data that already exists — new Feedback rows, status transitions, inline comments (finding 3), released marks (finding 5), regrade responses (finding 8), new versions by a group member via ProjectGroup — each with an explicit next action. Rename the badges to instructions: REJECTED becomes 'Changes requested — 3 comments to address'. On student/submission-detail.html, lead with the rubric breakdown and the unresolved anchored comments as a checklist, then the overall comment, then AI insight and related work below the fold, so machine output never outranks the lecturer's. Where marks are withheld, show the marking state explicitly rather than a blank — this doubles as the empty-state work already on the roadmap, and every one of these surfaces should be service-worker cached since the students most in need of 'what is due' are the ones with the worst connectivity.
- **why** A submission app that shows state instead of next actions gets checked once a week; one that shows what is due and what changed gets checked daily, which is the difference between a project and a product. Every input needed for this feed already exists in the schema — this is a rendering and ordering problem, not a data problem, which makes it unusually high value per unit of effort.

### Command palette (Ctrl/Cmd+K) as the single entry point to a 39-screen IA
- **severity** high · **effort** M · **category** navigation / keyboard-first
- **evidence** https://docs.github.com/en/enterprise-cloud@latest/get-started/accessibility/github-command-palette ; https://github.blog/changelog/2021-10-27-command-palette-beta/ ; https://uxpatterns.dev/patterns/advanced/command-palette ; https://help.superhuman.com/hc/en-us/articles/45191759067411-Speed-Up-With-Shortcuts
- **detail** GitHub's palette (Ctrl+K) is the closest analogue to UniSubmit's shape: it is *scoped* - it shows your current location top-left and uses it as the search scope, and it uses sigil modes (> for commands, # for issues, @ for people, / for files) so one input serves both navigation and action. Superhuman treats Cmd+K as 'the most important shortcut' and the thing that makes the product learnable, because a beginner can find any command by typing rather than by memorising 40 single-key bindings. Linear's palette is the canonical 'find things AND do things' surface. Feasibility in server-rendered Thymeleaf: ACHIEVABLE, and this is the rare case where the server-rendered version is genuinely easier, because your command list is static and role-known at render time. There is no SPA requirement - the palette is a filter over a list plus window.location assignment.

FULL version: a `/api/v1/palette?q=` endpoint returning role-scoped JSON (static routes + live matches on submission title, admission number, unit code, student name), debounced at 200ms, rendered into an existing `<dialog>` element. ~150 lines of JS in app.js plus one controller. CHEAP version (do this first, it is 80% of the value at 20% of the cost): serialise the role-appropriate route list into a `<script type="application/json" id="paletteCommands">` block inside layout.html via Thymeleaf `th:each`, filter it client-side with a substring match, zero new endpoints, zero network calls, works offline under the existing service worker. ~70 lines. Add live entity search later as a second data source.

Do NOT ship it as the only path to anything - it must stay a shortcut over the existing navbar, which also keeps it accessible.
- **do** Add a `<dialog id="cmdk">` to A:\unisubmit-main\unisubmit-main\src\main\resources\templates\layout.html (and admin/layout.html), populated from a Thymeleaf-rendered JSON island of role-visible routes. Add an `initCommandPalette()` function to A:\unisubmit-main\unisubmit-main\src\main\resources\static\js\app.js following the existing init* convention, bound to Ctrl+K / Cmd+K and to a visible 'Search or jump to...' button in fragments/navbar.html so it is discoverable without keyboard knowledge. Phase 2: a `PaletteController` at /api/v1/palette that unions submissions, students (admission number), units and programmes, security-filtered by role, LIMIT 8 per group. Show the current scope (e.g. 'CS 2024 / Semester 1') as a chip at top-left, GitHub-style, since CLASS_REP and lecturer authority is already programme+year+semester scoped.
- **why** UniSubmit has 30 controllers and 39 templates behind a role-gated navbar. An admin who needs 'Programmes' must remember whether it lives under the academic tree or the import flow. A palette collapses the whole information architecture into one text box, and it is also the single highest-signal demo moment in a hackathon judging session - it makes a Thymeleaf app feel like Linear.

### Autosave + local draft recovery on every long-form textarea (mobile connections drop)
- **severity** high · **effort** S · **category** data safety / inline editing
- **evidence** https://primer.style/ui-patterns/saving ; https://ui-patterns.com/patterns/autosave ; https://www.nngroup.com/articles/efficiency-vs-expectations/ ; https://medium.com/design-bootcamp/the-ux-shift-every-modern-platform-must-get-right-auto-save-28247b7d4fd2
- **detail** Notion, Figma and Google Docs have made 'no save button' the default expectation, but the research is emphatic on one caveat: removing the save button without a visible confirmation destroys trust - users panic when nothing tells them the work is safe. Primer's saving pattern and the ui-patterns autosave entry both prescribe the same resolution: autosave, then show an unobtrusive inline status ('Saved 14:32'), and keep an explicit submit for the *commit* action. NN/g frames it as not prioritising efficiency over expectations - the mechanism can change, the reassurance cannot.

Feasibility in Thymeleaf: ACHIEVABLE, and the cheap version needs NO server work at all. CHEAP version: a `initDraftRecovery()` in app.js that, on `input` (debounced 800ms), writes `{formAction}:{fieldName}` -> value into localStorage; on page load, if a stored value exists and the field is empty, render an inline bar above the field reading 'Unsaved draft from 12 minutes ago - Restore / Discard'. Clear the key on successful submit (detect via the redirect landing page, or clear on `submit`). ~40 lines, no endpoint, no DB column, and it works offline, which pairs naturally with the existing PWA service worker. FULL version: add a `feedback_draft` table plus `POST /api/drafts/{submissionId}` so a draft written on a phone reappears on a laptop, and render 'Saved HH:mm' from the server response. That is a day of work; the localStorage version is an hour and covers the actual failure mode (same device, lost connection or lost tab).

Apply the same treatment to the description/abstract textarea in student/new-submission.html and to announcement composition.
- **do** Add `initDraftRecovery()` to A:\unisubmit-main\unisubmit-main\src\main\resources\static\js\app.js, opt-in via a `data-autosave` attribute so it only attaches to fields that matter. Tag the feedback textarea in A:\unisubmit-main\unisubmit-main\src\main\resources\templates\lecturer\review-split.html (the 'Review decision' card, around the 'Draft feedback' AI button at line ~133), the description field in student\new-submission.html, and the composer in lecturer\announcements.html. Add a `.draft-restore-bar` style to base.css. Show a persistent 'Draft saved locally' microcopy line under the textarea so the reassurance is explicit - do not save silently.
- **why** A lecturer types 400 words of feedback in lecturer/review-split.html on a phone, the connection drops or the browser evicts the tab (a 512MB container plus a low-end Android is a realistic memory-pressure scenario), and the POST fails - the text is gone with no recovery. This is silent, unrecoverable user-data loss on the app's single most effortful action. It is also the cheapest high-severity fix in this list.

### Determinate progress for bulk import and AI analysis - stop reloading the whole page
- **severity** high · **effort** M · **category** progress / perceived performance
- **evidence** https://www.eleken.co/blog-posts/progress-indicator-ux ; https://learn.microsoft.com/en-us/windows/apps/develop/ui/controls/progress-controls ; https://vercel.com/docs/deployments/logs ; https://usersnap.com/blog/progress-indicators/
- **detail** The rule from the usability literature is graded by duration: users notice waits over 1 second; anything past ~2 seconds needs feedback; past ~10 seconds you need a *percent-done* indicator, not a spinner, because indeterminate feedback is what makes waiting feel long - uncertainty, not duration. Determinate indicators are specifically prescribed where the total is knowable, and a CSV import is the textbook case: you counted the rows during the preview phase. Vercel's deployment UI is the reference for the second half of the pattern - it doesn't just show a bar, it *streams the log* so the user can see which step is running and can diagnose a failure themselves rather than filing a ticket.

Feasibility: ACHIEVABLE without SSE and without a SPA. Your import is already two-phase (`POST /students/preview` then `POST /students/apply` in AdminImportController), which is exactly the structure a progress UI needs - the preview already knows `validCount`. CHEAP version: make `/apply` create an `ImportJob` row (id, total, processed, status, error_count) and return a redirect to a job page; the @Async worker updates `processed` every 25 rows; the job page polls `GET /admin/import/job/{id}` every 1.5s and updates a determinate `<progress>` element plus a 'Row 412 of 1,240 - 3 skipped' line. No SSE, no WebSocket, ~1 day. You already have @Async in AIInsightProcessingService, so the infrastructure exists.

Separately, fix the reload: have `/api/ai-insights/{id}` return the rendered Thymeleaf fragment (`fragments/components :: aiInsightPanel`) instead of JSON, and swap `panel.innerHTML` - this is the single-line htmx-style move that removes a full page load. Reuse the `.skeleton` classes already in base.css (lines ~1215-1230) for the pending state instead of an empty card, and surface a real message when the 40-attempt cap is hit ('Still working - refresh in a minute') rather than going quiet.
- **do** In A:\unisubmit-main\unisubmit-main\src\main\java\com\unisubmit\controller\...\AdminImportController.java, change the `/students/apply`, `/lecturers/apply` and `/academic/apply` handlers to enqueue an ImportJob and redirect to a progress page; add a polling status endpoint. Render the progress UI in admin\import.html using a native `<progress>` element (free accessibility, zero CSS). In app.js, rewrite `initInsightPolling()` to fragment-swap rather than `window.location.reload()`, show `.skeleton` placeholders while PENDING, and replace the silent give-up at MAX_ATTEMPTS with a visible retry affordance. Show elapsed time and rows/sec once past 10 seconds.
- **why** Bulk import of a 1,200-row student CSV and the AI insight pipeline are the two operations that exceed ten seconds, and both currently give the user nothing to reason about. The existing insight poller in app.js fires `window.location.reload()` on completion - a full HTML round trip every time, on a metered Kenyan mobile connection, and it silently gives up after 40 attempts with no message to the user. An admin watching a blank page during a 1,200-row import cannot tell a slow import from a hung one, and will hit Back or re-submit.

### Empty-state taxonomy with a mandatory CTA slot - four variants, one fragment
- **severity** high · **effort** M · **category** empty states
- **evidence** https://primer.style/ui-patterns/empty-states ; https://mobbin.com/glossary/empty-state ; https://www.eleken.co/blog-posts/empty-state-ux ; https://plghandbook.com/empty-state-design/
- **detail** Primer (GitHub's system) codifies empty states as distinct variants, not one component: first-use, no-results-from-search, post-completion, and blocked/no-permission. The design rule everyone converges on is that the title and description must answer 'what do I do now', and there must be a dominant single CTA - identify the one action successful users take first and make it the visually loudest thing on the page, to avoid decision paralysis. The Notion move worth stealing specifically: its empty state offers *templates*, not just a 'create' button, so the first action produces something real instead of another blank page. GitHub's post-completion variant (mascot illustration, 'take a break' copy) is the one people forget - it is a reward, not an error.

UniSubmit already has the raw materials: `fragments/components :: emptyGlyph(name)` and `.empty-state` CSS. What is missing is the contract. Feasibility: FULLY ACHIEVABLE, this is pure Thymeleaf.

The Notion-templates insight maps unusually well onto your import-order problem: the empty state on admin\faculties.html should not just say 'No faculties'. It should offer three ranked actions - 'Download the CSV template' (primary), 'Import faculties' (secondary), 'Add one manually' (tertiary) - plus one line naming what unlocks next ('Faculties are the first level; departments come next'). That turns the empty state into the ordering documentation for the nine-step import, delivered exactly where the confusion happens rather than in a wizard the admin has already dismissed.

Critically, distinguish the filter case: student\dashboard.html already has a separate `#filterEmptyState` (line ~105) alongside the first-run state at line ~41 - that is the correct pattern and it should be the template for the other 16 screens, with the filter variant offering 'Clear filters' rather than 'Create'.
- **do** Add `th:fragment="emptyState(variant, glyph, title, body, ctaHref, ctaLabel, secondaryHref, secondaryLabel)"` to A:\unisubmit-main\unisubmit-main\src\main\resources\templates\fragments\components.html, wrapping the existing emptyGlyph, with `variant` in {first-use, no-results, complete, blocked}. Sweep the 17 templates lacking empty states to use it. Prioritise the academic-tree screens (admin\faculties.html, departments.html, programmes.html, units.html, curricula.html) with the template-download-as-first-action treatment, since those empty states double as import sequencing guidance. Use the 'complete' variant on lecturer\dashboard.html when the review queue is cleared - it is cheap and it is the moment lecturers will remember.
- **why** Adding empty states screen-by-screen (already on the roadmap) will produce 17 inconsistent one-offs. The higher-leverage move is to make it structurally impossible to ship a blank region: one fragment that *requires* a primary action argument, plus the four-variant taxonomy that distinguishes 'you have nothing yet' from 'your filter matched nothing' - because the two need opposite copy and opposite buttons, and conflating them is the most common empty-state bug.

### Persistent, derived setup checklist on the admin dashboard - not a modal wizard
- **severity** high · **effort** M · **category** onboarding / progressive disclosure
- **evidence** https://docs.stripe.com/stripe-apps/onboarding ; https://getperspective.ai/blog/stripe-ai-customer-onboarding-philosophy-lessons-from-a-conversion-obsessed-company ; https://www.appcues.com/blog/user-onboarding-ui-ux-patterns ; https://userpilot.com/blog/progressive-disclosure-examples/
- **detail** The framing worth stealing from Stripe is that its onboarding is a long, multi-session conversation masquerading as a dashboard - there is no separate wizard mode to exit. Checklists are the most effective progressive-onboarding device because they decompose a large task into visible increments and give a sense of forward momentum; progressive disclosure means revealing the next step only after the previous one succeeds, rather than dumping all nine imports at once.

The cheap engineering insight, which is what makes this a 1-2 day job rather than a week: DERIVE the checklist state, do not store it. Every step maps to a COUNT query you can already run - faculties > 0, departments > 0, programmes > 0, units > 0, students > 0, lecturers > 0, at least one assignment, branding logo uploaded, at least one submission received. No `setup_progress` table, no completion flags to keep in sync, no bug where a deleted faculty leaves a stale green tick. Steps with unmet dependencies render disabled with the reason inline ('Add a faculty first'), which encodes the confusing import order directly into the UI - the dependency graph becomes the interface.

Feasibility: FULLY ACHIEVABLE, zero JS. It is a Thymeleaf `th:each` over a list of records built in AdminDashboardController plus a `<progress>` element showing '4 of 9 complete'. Auto-collapse the card (via `<details>` with `th:attr="open=${incompleteCount > 0}"`) once everything is done, so it fades out of the way instead of needing a dismiss button and a stored preference.
- **do** Add a `SetupStep` record (label, done, href, blockedReason) and a `List<SetupStep> setupSteps` model attribute computed from repository counts in A:\unisubmit-main\unisubmit-main\src\main\java\com\unisubmit\controller\AdminDashboardController.java. Render it as the top card of A:\unisubmit-main\unisubmit-main\src\main\resources\templates\admin\dashboard.html, above the 'Quick actions' card (currently line ~65) - the zeros the admin is staring at become the checklist's evidence rather than a dead end. Wrap it in `<details>` so it self-collapses at 100%. Reuse the same derived-steps idea as a lightweight 3-step version on student\dashboard.html ('Join your unit / Upload your first draft / Get AI insights').
- **why** A first-run wizard is on the roadmap, but a wizard is a one-shot, dismissible, stateful thing that an admin will close on day one and never see again - and school setup genuinely spans multiple sessions (you import faculties on Monday and students when the registrar sends the file on Thursday). The pattern that actually fits a multi-session setup is Stripe's: the dashboard *is* the onboarding, permanently, and it recomputes itself from real data.

### Undo-after-the-fact instead of confirm-before - and promote toasts off phones
- **severity** medium · **effort** M · **category** error recovery / feedback
- **evidence** https://uxplanet.org/confirmation-dialogs-how-to-design-dialogues-without-irritation-7b4cf2599956 ; https://blog.logrocket.com/ux-design/double-check-user-actions-confirmation-dialog/ ; https://www.magicbell.com/blog/what-is-a-toast-message-and-how-do-you-use-it ; https://stuff.mit.edu/afs/sipb.mit.edu/project/android/docs/design/patterns/confirming-acknowledging.html
- **detail** Gmail's model is the canonical one: perform the action immediately, show a toast ('Conversation moved to Trash') with an Undo affordance for a few seconds, and let silence mean commit. The UX literature is direct - when an action is reversible, a confirmation dialog adds an unwanted interaction step and should be replaced by undo. The subtlety Gmail adds with Undo Send is a configurable *delay window*, which matters here: a lecturer who has just fired a rejection to a student would benefit from a 10-second hold before the notification actually goes out.

Feasibility: ACHIEVABLE server-rendered, and notably this needs NO optimistic UI - that is the part people assume requires a SPA and doesn't. The action really does complete on the server; the undo affordance simply rides in the flash message on the redirect. Implementation: the mutating POST writes an `UndoToken` (action type, entity id, prior state snapshot, expiry ~20s) and puts the token in flash attributes; the redirect target renders an alert containing a small `<form method="post" action="/undo/{token}">` with a single 'Undo' button. One `UndoController`, one short-lived table (or session attribute, since it is single-school and short-lived). Because AuditLog already records STATUS_CHANGED with prior state, the snapshot for the highest-value case (review decision) is close to free.

One prerequisite fix: `.alert.toast-mode` in base.css (line ~2432) is gated to phones only. Undo affordances must be visible on desktop too - that is where lecturers do grading. Promote toast-mode to all viewports, and make the undo toast persist longer than the standard flash (do not auto-dismiss at 3s if it contains an action).

Where NOT to use it: account deletion and anything touching credentials should keep an explicit confirm. Undo is for the reversible middle ground.
- **do** Add an `UndoController` at /undo/{token} plus a `flash-undo` alert variant in A:\unisubmit-main\unisubmit-main\src\main\resources\templates\fragments\alerts.html. Wire it to: the review-decision POST behind lecturer\review-split.html (10-second hold before the student notification is dispatched - the highest-value case), 'Mark all read' in notifications.html (line ~9), submission archive on student\submission-detail.html, and the delete actions on the admin academic-tree screens. In base.css, lift the `.alert.toast-mode` rules out of the phone-only media query so undo is reachable on desktop.
- **why** Consequential actions in UniSubmit (submitting a review decision, archiving a submission, 'Mark all read' on the notifications page, deleting a node of the academic tree) are irreversible-feeling and currently guarded by nothing or by a browser confirm. Confirm dialogs tax the 99% of correct actions to protect against the 1% wrong one, and users learn to click through them blind. Undo taxes nobody.

### Keyboard triage for the lecturer review queue (Superhuman J/K/E, plus '?' help)
- **severity** medium · **effort** S · **category** keyboard-first / throughput
- **evidence** https://help.superhuman.com/hc/en-us/articles/45191759067411-Speed-Up-With-Shortcuts ; https://blog.superhuman.com/email-triage/ ; https://blog.superhuman.com/inbox-zero-in-7-steps/ ; https://www.hashbuilds.com/patterns/what-is-command-palette
- **detail** Superhuman's whole thesis is this loop: E to mark done, R to reply, single-key actions that eliminate the mouse and collapse per-item processing time, framed around a triage model (do it, defer it, delegate it, delete it) where each disposition is one keystroke. Gmail and Linear use the same J/K list traversal. The reason it works is that the decision is already made in the user's head before their hand moves - the UI's only job is to not be in the way.

Feasibility: FULLY ACHIEVABLE and cheap, because the actions already exist as real DOM elements. review-split.html already renders `<button data-review-action="APPROVED|REJECTED|UNDER_REVIEW">` (lines ~119-127) and app.js already has `initReviewActions()` selecting them. The keyboard layer is literally a keydown handler that calls `.click()` on elements that are already there - it is progressive enhancement in the purest sense: JS off, everything still works by mouse. ~30 lines.

Bindings: j/k to move between submissions in the lecturer\dashboard.html queue (server already renders the list; the handler just moves focus and follows the anchor on Enter), a = approve, r = request changes, u = under review, c = focus the comment box, Escape to blur. Guard every binding with a check that the event target is not an input/textarea/contenteditable, otherwise typing feedback will trigger actions - this is the one bug that makes teams abandon the pattern.

Discoverability is mandatory or nobody uses it. Two devices, both cheap: a `?` overlay listing the shortcuts (a `<dialog>` with a static Thymeleaf table, no JS beyond open/close), and `<kbd>` hints rendered inside the buttons themselves so the binding is learned passively - 'Approve <kbd>A</kbd>'. Note base.css has no `.kbd` style yet; add one.
- **do** Add `initReviewShortcuts()` and `initShortcutHelp()` to A:\unisubmit-main\unisubmit-main\src\main\resources\static\js\app.js, extending the existing `initReviewActions()` at line ~90 rather than duplicating selectors. Render `<kbd>` hints inside the three review buttons in A:\unisubmit-main\unisubmit-main\src\main\resources\templates\lecturer\review-split.html and add prev/next submission links (server-rendered, so j/k has something real to follow). Add a `<dialog id="shortcutHelp">` fragment to fragments\components.html, opened by `?` and by a discoverable button in the navbar. Add `.kbd` styling to base.css.
- **why** A lecturer marking 60 submissions performs the identical loop 60 times: open, read, decide, comment, next. Every mouse trip to the 'Approve' button and back to the list is a context switch that compounds. This is the single screen in UniSubmit where a power user does the same thing dozens of times in a sitting, which is exactly the condition under which keyboard bindings pay off.

### Notification rollup and per-type mute before the feed becomes wallpaper
- **severity** medium · **effort** M · **category** notifications
- **evidence** https://www.courier.com/blog/how-to-reduce-notification-fatigue-7-proven-product-strategies-for-saas ; https://www.suprsend.com/post/notification-batching-and-digest ; https://knock.app/manuals/slack-notifications/best-practices-for-slack-notifications ; https://www.courier.com/blog/how-slack-builds-smart-notification-systems-users-want
- **detail** Slack's operating principle is that too many notifications turn a productivity tool into constant interruption, and the fix is threefold. (1) Batch and digest: instead of 'Server 1 restarted / Server 2 restarted / Server 3 restarted', send one scannable summary. The measured effect is counter-intuitive and worth quoting to a judging panel - a digest with 8 meaningful updates gets *higher* open rates than 8 individual pings, because the signal-to-noise ratio is what determines whether people trust the channel at all. (2) Frequency capping: cap notifications per user per window and queue the overflow into a digest, because one user generating fifty events in an hour trains everyone else to ignore you. (3) Granular per-type control, which measurably reduces opt-outs versus an all-or-nothing switch.

Feasibility: FULLY ACHIEVABLE with zero JS - this is a query change plus a Thymeleaf change. CHEAP version, no schema change: group at read time in AppNotificationRepository with a `GROUP BY type, submission_id, DATE(created_at)` projection and render 'Feedback on 3 submissions - today' as one expandable row (`<details>`), with the individual rows inside. The unread count becomes the count of *groups*, not events. FULL version: add a `notification_preference` table with a per-type channel toggle (in-app / email / off) and a settings screen, plus a nightly digest job for DEADLINE using the @Async infrastructure already present.

One timing rule worth adopting given the deployment context: non-urgent notifications should not fire at 3am local time - defer them to a morning batch. Cheap to implement, and it is the kind of detail that reads as product maturity.
- **do** Add a grouped projection query to A:\unisubmit-main\unisubmit-main\src\main\java\com\unisubmit\repository\AppNotificationRepository.java and render collapsed groups in A:\unisubmit-main\unisubmit-main\src\main\resources\templates\notifications.html (currently 45 lines, one flat list). Keep the existing 'Mark all read' but pair it with the undo toast from the undo finding, since it is currently irreversible. Phase 2: a preferences screen with a row per NotificationType, and a frequency cap in NotificationService that rolls the overflow into a single digest row. Before expanding NotificationType beyond three values, add the grouping - otherwise every new type multiplies the noise.
- **why** NotificationType currently has three values (NEW_FEEDBACK, STATUS_CHANGE, DEADLINE) and notifications appear to be written one row per event. In a 200-student cohort, a lecturer batch-reviewing produces a wall of near-identical rows, and a student near a deadline gets pinged for every status transition. Once a user learns their notification bell is noise, they stop opening it - and then the DEADLINE notification, the one that actually matters, gets ignored too. That failure is not recoverable by adding more notifications.

### Promote the existing AuditLog into a school-wide activity feed - the trust surface you already paid for
- **severity** medium · **effort** S · **category** transparency / trust
- **evidence** https://docs.github.com/en/organizations/keeping-your-organization-secure/managing-security-settings-for-your-organization/reviewing-the-audit-log-for-your-organization ; https://uxpatterns.dev/patterns/social/activity-feed ; https://uxpatterns.dev/patterns/data-display/timeline ; https://linear.app/changelog ; https://blog.getsimpledirect.com/linears-changelog-strategy-a-deep-dive-and-what-you-can-learn/
- **detail** GitHub draws the line precisely, and it is the right line for UniSubmit: the *activity feed* is for collaboration events (pushes, PRs), the *audit log* is for administrative and security actions (permission changes, SSO, settings). They are different audiences and belong on different screens. Each audit entry carries timestamp, actor, and nature of the change; the trust property comes from it being append-only, which yours already is. The generic pattern framing is that a timeline exists so users can reconstruct a sequence and audit a process.

Second, distinct half of this finding - the in-product changelog. Linear's changelog gets over 60% monthly engagement against a 10-15% SaaS norm, because each entry is a headline plus a short narrative of the design thinking plus one screenshot, and because the cadence is predictable - gaps erode trust, since users wonder what changed in the versions you skipped. Vercel treats it as a confidence-building channel for developers. For a single-school deployment this matters more than it sounds: the admin needs to know why a screen moved between the version they trained staff on and the one running today.

Feasibility: BOTH FULLY ACHIEVABLE server-rendered. The activity feed is a paginated Thymeleaf table over an existing repository. The changelog is a static Thymeleaf page (or a markdown file rendered at build), plus - the one piece with any subtlety - an unobtrusive 'What's new' dot in the navbar driven by comparing a `last_seen_changelog_version` on the user against a constant. Do not build a modal that interrupts login; the research on interruption applies here too.
- **do** Extend AuditAction (A:\unisubmit-main\unisubmit-main\src\main\java\com\unisubmit\domain\AuditAction.java) with administrative events: IMPORT_APPLIED, ACCOUNT_CREATED, ROLE_GRANTED, ROLE_REVOKED, BRANDING_CHANGED, ACADEMIC_NODE_DELETED, PASSWORD_RESET_ISSUED - and call AuditService from AdminImportController, AdminAccountController and AdminBrandingController, which are the three places an unexplained change originates. Add an `/admin/activity` screen with actor + action + target + timestamp, filterable by actor and action, reusing the existing `.table` styles and the feedbackTimeline fragment's visual language. Separately add a `/changelog` page and a subtle unread dot in fragments\navbar.html - no modal. The activity feed also gives the admin dashboard's zeros something to link to once data exists.
- **why** UniSubmit already has AuditLog, AuditAction, AuditLogRepository and AuditService, but AuditAction has only 5 values and they are all submission-scoped, and the feed is only surfaced inside review-split.html's 'Activity timeline' card. The highest-stakes questions in a coursework system are institutional, not per-submission - 'who changed this student's programme', 'who ran the import that created 340 duplicate accounts', 'who granted CLASS_REP to this person'. Answering those is a small amount of work on infrastructure that already exists, and it is disproportionately persuasive to a project examiner assessing accountability.

### Inline row editing on the admin CRUD screens - kill the list-then-separate-form round trip
- **severity** medium · **effort** S · **category** inline editing
- **evidence** https://github.com/wimdeblauwe/htmx-spring-boot ; https://tucanoo.com/htmx-with-spring-boot-and-thymeleaf-a-production-grade-guide-beyond-the-to-do-app/ ; https://spring.io/blog/2024/03/15/hypermedia-and-browser-enhancement/ ; https://www.925studios.co/blog/linear-design-breakdown-saas-ui-2026
- **detail** This is the finding where I want to be most explicit about the SPA question, because it is the pattern most often assumed to need one. It does not.

ZERO-JS version (ship this): render a collapsed `<details>` inside each table row containing a pre-filled edit form that POSTs to the existing update endpoint and redirects back to `/admin/units#row-42`. The browser restores scroll to the anchor for free. No JS, no new dependency, works with the service worker, and the markup change is confined to one `th:each` block per screen. Cost: a few hours per screen, less if you extract a shared fragment. The tradeoff versus true inline editing is a page reload on save - acceptable, and vastly better than today.

TRUE inline version: htmx is the standard answer in the Spring/Thymeleaf world and is designed for exactly this - the controller returns a Thymeleaf *fragment* (`units :: row`) instead of a full page, and htmx swaps the row in place. wimdeblauwe/htmx-spring-boot provides `@HxRequest` and an `hx:` Thymeleaf dialect so a controller can serve both the full page and the fragment from one method. Two caveats before adopting: (1) it is ~14KB of new dependency against a minimal-JS principle and a 512MB container - defensible, but it is a real architectural commitment; (2) Spring Security CSRF will silently break htmx POST/PUT/DELETE unless the token is wired into `hx-headers` - budget for that, it is the standard first bug.

Recommendation: do the zero-JS `<details>` version now, and only reach for htmx if you later find yourself wanting fragment swaps in three or more places (the AI insight panel from the progress finding would be the second, the notification list the third - at which point htmx pays for itself and also cleans up initInsightPolling).
- **do** Extract a shared `th:fragment="editableRow(...)"` into A:\unisubmit-main\unisubmit-main\src\main\resources\templates\fragments\components.html using the `<details>`-in-row approach, and apply it across admin\faculties.html, departments.html, programmes.html, units.html, tags.html and curricula.html. Have the update handlers redirect to `...#row-{id}` so position is preserved. Note that admin screens already have a client-side table filter (`initTableSearch()` in app.js line ~140) - make sure expanded rows interact correctly with it (a filtered-out row must not stay open). Defer htmx until there is a third fragment-swap use case; if adopted, add the CSRF token to a global `hx-headers` on `<body>` in layout.html on day one.
- **why** The academic-tree screens (faculties, departments, programmes, units, tags, curricula) are all table-plus-form pages. Fixing a typo in one unit code costs a page load, a scroll, a form fill, a submit, and a return that loses your scroll position - and setting up a school means doing this dozens of times in a row. Linear and Notion set the expectation that you edit the thing where it sits.


## Lens: hackathon-judging + ux-accessibility + flows-and-gaps

### Check the event's prior-work rule before you enter — MLH's standard rules would disqualify the whole app
- **severity** critical · **effort** XS · **category** eligibility
- **evidence** https://github.com/MLH/mlh-policies/blob/main/standard-hackathon-rules.md ; https://guide.mlh.com/general-information/judging-and-submissions/rules-for-your-hackathon ; A:/unisubmit-main/unisubmit-main/CODEBASE-MAP.md:11-31 (181 Java files, ~17.4k LOC, months of history); A:/unisubmit-main/unisubmit-main/ROADMAP-ARCHIVE.md (51KB of prior phases)
- **detail** MLH's standard rules say a team "cannot submit a project that includes prior work." Teams may reuse an *idea* they had before the event, and may work on an idea they have worked on before **"as long as they do not re-use code or other project materials."** The rules also explicitly call out open-sourcing a pre-built project in order to use it during the event as against the spirit of the rules. The one carve-out that helps you: adding new features to an existing project is allowed, and in that case "judges only consider new functionality introduced or new features added during the hackathon."

UniSubmit is 181 Java files, ~17.4k LOC, 39 templates, a completed V3 roadmap and a 51KB archive. Submitted as a whole it is prior work under those rules. MLH also notes organizers may fork and edit the rules, so the governing document is the specific event's rules page, not MLH's — but you must read it, and the answer changes what you submit, not just how you pitch it.

This is upstream of every other item here. Everything else assumes you are eligible.
- **do** Read the specific event's rules page today and classify it: (a) MLH standard unmodified, (b) forked/edited, (c) a capstone/FYP showcase with no build-window rule. If (a) or (b) restricts prior work, do not submit UniSubmit as the project. Instead scope the entry to something genuinely built inside the window — the first-run onboarding layer (ROADMAP-V4 1.2 checklist + 1.7 sample data + 1.8 empty states) is the natural candidate because it is unbuilt, demoable, and self-contained — and present the existing platform explicitly as pre-existing context in the first sentence of the README and the first ten seconds of the demo. Volunteering the boundary reads as integrity; being caught at it reads as cheating. If (c), ignore this finding entirely and treat the whole app as fair game.
- **why** A disqualification after the demo costs you the entire event, and the failure mode is silent — organizers run cheating checks on finalists (per MLH's judging plan), so the risk lands precisely when you are winning. It also determines the scope of the demo: if only new work counts, the two-minute narrative has to be about the new work, not the platform.

### Rebuild the demo around the one memorable image — MLH stack ranking gives zero points for being broadly solid
- **severity** critical · **effort** S · **category** demo-narrative
- **evidence** https://guide.mlh.com/general-information/judging-and-submissions/judging-plan ; https://angelhack.com/blog/10-tips-to-help-you-rock-your-next-hackathon-demo/ ; https://blog.jetbrains.com/ai/2026/06/how-to-win-a-hackathon-notes-from-the-judging-table/ ; A:/unisubmit-main/unisubmit-main/ROADMAP-V4.md:266-290
- **detail** MLH's official plan: science-fair format, **4 minutes total per team — 2 minutes demo, 1 minute questions, 1 minute travel**, roughly 3 judging rounds. Scoring is **stack ranking**: each judge reports only their *top 3* projects, worth 3/2/1 points. The arithmetic is brutal and non-obvious — a project every judge rates "very good, 4th best" scores **exactly zero**. You are not optimising average quality; you are optimising the probability that one judge cannot stop thinking about you.

Against that, ROADMAP-V4 §7's five-beat narrative is 30+60+20+90+30 = **230 seconds against a 120-second budget**, and it buries the memorable beat at position 3. AngelHack's breakdown for a 2-minute pitch is 30s problem/solution, **60s demo**, 30s next steps. The JetBrains judging-table write-up is blunter still: you have about 90 seconds to show something working, and "one clear before/after contrast is more persuasive than a feature tour."

Your before/after is beat 3 — same codebase, two schools, side by side, unrecognisable as the same product. That is the image. It is currently fourth in line, behind an empty-room beat that only lands if Phase 1 ships.
- **do** Re-cut to a 105-second table demo with 15s of slack, in this order: **(1) 0:00–0:20 — the money shot first.** Two browser windows already open, two schools, same app. Say one sentence: "Same codebase. Two universities. One upload." Do not explain yet. **(2) 0:20–0:35 — the problem, now that they care.** One line about Kenyan universities running coursework on WhatsApp and email. **(3) 0:35–1:20 — one continuous flow**, student uploads → lecturer sees AI insight and grades → collaborator surfaced. One flow, no menu tours. **(4) 1:20–1:45 — the measured claim** from the next finding, on screen as a number. Then stop talking.

Cut from the table demo entirely: the empty-room beat (it is a *pitch* point, not a demo point — put it in the README), the technical-depth beat (see the foreground/hide finding), and any navigation of the admin tree. Per AngelHack, never demo a login screen — be pre-authenticated in two browser profiles with the tabs already warm. Rehearse aloud with a timer at least twice; JetBrains lists "demos that stall" as a top failure mode, and Nick Singh's guide adds the mundane killer: turn on Do Not Disturb.
- **why** Under stack ranking, ordering is not a stylistic choice — it decides whether you appear on a judge's top-3 card at all. Judges see dozens of projects back to back; the one thing they carry into deliberation is a single image, and yours arrives 110 seconds in, after their attention is spent.

### Convert the theming engine from a novelty claim into a measured result — that is what scores on both rubrics
- **severity** high · **effort** S · **category** measurable-impact
- **evidence** https://github.com/MLH/mlh-policies/blob/main/standard-hackathon-rules.md (criteria: Technology, Design, Completion, Learning) ; https://www.cst.cam.ac.uk/teaching/part-ii/projects/assessment (Evaluation & Conclusions = 25 marks) ; https://research.cgu.edu/hackathon/home/judging-rubric/ ; A:/unisubmit-main/unisubmit-main/src/main/java/com/unisubmit/service/BrandingService.java:22-46 ; A:/unisubmit-main/unisubmit-main/ROADMAP-V4.md:226,286-288
- **detail** MLH's judged criteria are **Technology, Design, Completion, Learning** — and the rules note that code quality, pitch quality and *idea originality* are explicitly **not** among them. So "we built a novel theming engine" earns nothing on its own; "here is the engine working, and here is the number proving it works" earns under Technology *and* Design. Cambridge's Part II scheme puts 25 of 100 marks on **Evaluation and Conclusions**, where the top band needs a "compelling argument demonstrating success criteria met." CGU's rubric gives a full 20% to explainability of outputs. All three reward the same artifact: a measurement.

ROADMAP-V4 already gestures at this ("#3B82F6 before → #E8792B after, on the same crest") and correctly notes judges reward evidence over claims — but it is one anecdote about one crest, and Phase 4.6 defers the contrast test behind an L-effort tonal migration that will not land in the final stretch.

BrandingService already sanitises ~35 colour tokens and enforces a hex allow-list; the WCAG check already exists as an after-the-fact warning. Everything needed to produce a real result set is built.
- **do** Spend half a day producing one table and one image. Take 8–10 real university crests (Kenyan universities first — UoN, Kenyatta, JKUAT, Strathmore, Moi, Egerton, plus two international for contrast). Run each through the extractor and record, per crest: dominant colour extracted, number of the ~35 sanitised tokens generated, and **how many role pairings pass WCAG AA 4.5:1 before vs after the extraction fix**. Publish it as a small table in the README and as one on-screen slide/panel in the demo — "10 crests, 340 token pairings, AA pass rate 61% → 100%."

Do **not** do the Phase 4 tonal migration to get this (see the cut list). Lift only step 4.6's assertion — a JUnit test that walks every role pairing at both light and dark assignments and asserts 4.5:1 — and run it across the 10 crests. That is a few hours, it produces the number, and it leaves a permanent regression test that itself reads as engineering maturity in the dissertation.
- **why** This is the single cheapest conversion of existing work into rubric points, and it works for the hackathon judge and the FYP examiner simultaneously — the only artifact in this list that does. It also inoculates you against the most damaging judge question, "is the theming just a colour picker?", by answering it with a table instead of a claim.

### The README is the judging artifact and it currently describes a different product
- **severity** high · **effort** S · **category** documentation
- **evidence** A:/unisubmit-main/unisubmit-main/README.md:89-94 (Flyway listed in stack; "one dark design system"), README.md:107-118 (password123 table), README.md:125 ("43 unit tests" — actual count is 127 @Test), zero occurrences of brand/theme/palette/material in README.md ; https://info.devpost.com/blog/understanding-hackathon-submission-and-judging-criteria ; https://info.devpost.com/blog/hackathon-judging-tips
- **detail** Devpost's guidance is that judges review multiple projects back to back and screen the submission page before deciding what to look at closely; its judges' round-up adds that they verify basic requirements first and then actually try the thing. The README is therefore read *before* the demo, often by judges who never see you present at all.

Four concrete defects, all verifiable in the current file:

1. **The differentiator is absent.** `grep -i "brand|theme|palette|material"` over README.md returns nothing. The branding/theming engine — the one component nobody else has — does not appear anywhere in the document a judge reads first.
2. **README.md:93 actively contradicts it**: "one dark design system in `static/css/base.css`". That was true two roadmap versions ago.
3. **README.md:125 claims "43 unit tests, all green."** The tree contains **127** `@Test` methods across 22 files. You are under-reporting your own strongest FYP evidence by a factor of three, and any judge who runs the suite catches a documentation error at the same moment.
4. **README.md:107-118 publishes an `admin` / `password123` credentials table** as the documented way to run the app, and README.md:89-90 lists Flyway in the stack when it is deliberately disabled. A security-minded judge reads the first as the product's auth model; the second is the kind of detail an FYP examiner probes in the viva.
- **do** Rewrite the top 40 lines only; the intelligence-story section below is genuinely good and should stay. New shape: **(1)** one sentence on the problem in a Kenyan university; **(2)** the money-shot image (same code, two schools) immediately, above the fold; **(3)** three bullets — branding engine with the measured contrast number, six-signal recommender with the precision@5 figure, graceful degradation with no API key; **(4)** a 60-second run path; **(5)** a one-line honesty note on what is not built. Fix the test count to 127, drop Flyway from the stack line and note it is disabled by design, and either delete the credentials table or fence it clearly as `local` profile only. Also update `CODEBASE-MAP.md` §8 to mention the branding surface — ROADMAP-V4 task 0.3 already flags it, and it is XS.
- **why** Devpost-style judging weights the written submission heavily because it is what survives the event. A README that omits your differentiator, under-counts your tests and advertises a hardcoded password is not a neutral omission — it converts three strengths into two errors and a blank.

### Foreground the evaluation harness; hide the security-chain and test-count depth at the table but lead with it in the dissertation
- **severity** high · **effort** S · **category** technical-depth
- **evidence** A:/unisubmit-main/unisubmit-main/src/main/java/com/unisubmit/service/EvaluationService.java:19-38 (precision@5, MRR, replay across weight configs) ; https://www.cst.cam.ac.uk/teaching/part-ii/projects/assessment (Preparation 30 / Implementation 45 / Evaluation 25) ; https://www.sussex.ac.uk/ei/internal/forstudents/informatics/undergraduate/finalyearprojects/reportmarkingcriteria ; https://github.com/MLH/mlh-policies/blob/main/standard-hackathon-rules.md
- **detail** Your two audiences score almost opposite things, and the same asset can be worth everything or nothing depending on which room it is in.

**MLH does not score code quality.** Its criteria are Technology, Design, Completion, Learning, and the rules say code quality, pitch quality and idea originality are not considered. So "two independent security chains, careful transaction discipline, 127 tests" scores approximately zero at the judging table — and worse, spending 30 of your 120 seconds on it is 30 seconds not spent on the memorable image. Devpost's judges do warn about the inverse failure (polished UI with no depth behind it), but they catch that in **Q&A**, not in the demo.

**Cambridge weights Implementation at 45 and Evaluation at 25 of 100.** Sussex's criteria reward "rigorous research and evaluation" and "evidence of extension beyond core requirements." The top Cambridge evaluation band requires a compelling argument that success criteria were met with excellent critical interpretation.

Here is the asymmetry worth exploiting: `EvaluationService` already replays the recommender under several weight configurations and scores each with **precision@5 and MRR**, using accepted collaboration requests as ground truth. That is an ablation study. It is the exact artifact Cambridge's 25 marks ask for, it is the thing almost no student project has, and it currently gets one line in the README and a buried admin page.
- **do** **At the judging table, hide:** the two security chains, the transaction discipline, the test count, the JWT/mobile API (`/api/v1` role prefixes are reserved but unpopulated — mentioning it invites a question you lose), and the pgvector/SPECTER plumbing. Keep them loaded as *answers*, not as *content* — see the Q&A finding.

**At the judging table, foreground:** the branding measurement, one working flow, and one honest limitation. Judges reward candour about limits; JetBrains and Devpost both list overclaiming as a losing move.

**In the dissertation, foreground:** a full Evaluation chapter built from `EvaluationService` — a table of weight configurations × precision@5 × MRR, the ablation showing which of the six signals actually carries the score, and an honest paragraph on N being small and the ground truth being self-selected (accepted requests are biased toward what was surfaced). Stating that limitation is worth more marks than hiding it. Add the contrast-pass table from the branding finding as a second evaluated criterion. Then add the security chains and the 127 tests to the Implementation chapter, where they are worth 45 marks instead of zero.
- **why** Most student projects lose the evaluation marks entirely because they have nothing to evaluate against. You already ran the experiment and did not write it up. Meanwhile the depth you *are* proud of is the depth the hackathon rubric explicitly ignores. Sorting these two into the right rooms costs nothing and moves both scores.

### Judges will open your link and test it themselves — a 120-second cold start into an empty room is a scored failure
- **severity** high · **effort** M · **category** live-deployment
- **evidence** A:/unisubmit-main/unisubmit-main/Dockerfile (HEALTHCHECK --start-period=120s) ; A:/unisubmit-main/unisubmit-main/deploy/RAILWAY.md:36 ("~1–2 min" first boot) ; A:/unisubmit-main/unisubmit-main/ROADMAP-V4.md:138-147 (task 1.7 in-app sample data, unbuilt) ; https://info.devpost.com/blog/hackathon-judging-tips
- **detail** Two of the five Devpost judges (Kelvin Boateng, Warren Marusiak) describe **actually using the project themselves** as part of their process, and Marusiak's stated test is "is that finished product something that I would want to use?" A live URL is not decoration; it is a second, unsupervised judging surface where you are not present to narrate.

Yours has three compounding problems. **Cold start:** the Dockerfile gives the app a 120-second health-check start period and RAILWAY.md documents ~1–2 minutes for first boot while Hibernate `ddl-auto` reconciles the schema against Supabase. A judge who clicks at 11pm on a sleeping free-tier container waits on a blank page. **Empty room:** ROADMAP-V4 1.7 is unbuilt, so even a warm instance has no in-app way to load sample data — and the documented alternative requires three env flags plus reintroducing the `password123` admin. **No guest path:** there is no read-only demo login, so the judge's first screen is a login form they cannot pass.

The unhelpful combination: your best asset (two visually distinct deployments) is exactly the thing a *live link* proves better than a video, and the link currently cannot deliver it.
- **do** In priority order. **(1) Ship a separate, pre-seeded, pre-branded *demo* instance and put that URL on the submission** — not the pristine one. It should boot already populated and already themed as a fictional university, so the first frame is the money shot. **(2) Keep it warm** for the whole judging window with an external uptime pinger hitting `/health` every 5 minutes; `/health` already exists and the endpoint is cheap. **(3) Print three copy-pasteable demo logins directly on the login page of the demo instance only**, behind a profile flag — a judge should never have to read a README to get in. **(4)** Do ROADMAP-V4 1.7 ("Load sample data", M) if and only if time remains after the README and video; the pre-seeded instance achieves 80% of its judging value without the code. **(5)** Put the second branded deployment behind a visible link from the first — "see this same build as another school" — so an unattended judge discovers the differentiator without you.
- **why** An unattended judge who bounces off a cold blank page scores you on that, and you never find out. It is also the cheapest place to lose Devpost's "Completion" and MLH's equivalent — the project genuinely works, and the deployment is what makes it look like it does not.

### Cut list: Phase 4, Phase 5 and the dead-code sweep are days of work worth zero to either rubric
- **severity** high · **effort** XS · **category** scope-cut
- **evidence** A:/unisubmit-main/unisubmit-main/ROADMAP-V4.md:201-231 (Phase 4, 3–4 days, two L steps, 4.3 flagged as load-bearing risk) ; ROADMAP-V4.md:233-251 (Phase 5, explicitly unvalidated) ; ROADMAP-V4.md:185-198 (Phase 3.3, 16 tables) ; CODEBASE-MAP.md:278-283 (Tier-2 dead code ~400 lines) ; https://blog.jetbrains.com/ai/2026/06/how-to-win-a-hackathon-notes-from-the-judging-table/ ; https://www.nicksingh.com/posts/win-hackathons-a-how-to-guide
- **detail** JetBrains' judging-table notes list "attempting too many features instead of perfecting one" as a top failure mode. Nick Singh's guide is more specific about final-stretch discipline: rank features beforehand and commit to the ranking; cut anything not on the demo path — he cuts public deployment and auth entirely for a weekend build.

Applied to ROADMAP-V4, four items are net-negative right now:

**Phase 4, the tonal design system (3–4 days, steps 4.2 and 4.5 both L).** The roadmap itself flags 4.3 as load-bearing: "if the alias table is wrong the whole UI shifts at once." No judge can distinguish a flat-token theme from a tonal one — both look themed. You would spend the most days on the change with the highest chance of breaking the demo and the lowest visible delta.

**Phase 5 in full** — rubric grading, originality reports, command palette, activity feed. The roadmap marks it unvalidated; the research notes correctly identify rubric grading and the originality report as the highest-value *product* features, but each is L effort and a half-built rubric grader demos worse than none. Note the command palette is the tempting one because notes/RESEARCH-NOTES.md:229 calls it "the single highest-signal demo moment" — it is not, because it competes with your money shot for the same 20 seconds and a judge who has seen Linear reads it as familiar rather than novel.

**Phase 3.3, responsive tables across all 16.** **Tier-2 dead-code removal (~400 lines of Framework/Database/Language/Skill tag families).** Zero judge visibility, both.
- **do** **Cut outright:** Phase 4 entirely (extract only step 4.6's contrast test — hours, not days — because that produces the measurable claim); Phase 5 entirely; Tier-2 dead code; the Flutter client (already correctly cut in §6, keep it cut and do not mention `/api/v1` in the demo). **Scope down:** Phase 3.3 to only the 2–3 screens on the demo path; Phase 1.8 empty states to only the screens a judge will actually see, not all 17.

**Spend the reclaimed days on, in order:** the README rewrite (S), the branding measurement table (S), the demo video (S), the pre-seeded demo deployment (M), ROADMAP-V4 1.2 setup checklist (S — it is the one Phase 1 item that is both cheap and visible), and 1.5 the per-row import transaction fix (S — small, and it is the kind of correctness defect an FYP examiner finds by reading the code, which is where it costs you). Freeze the codebase 24 hours before judging: MLH's rules allow minor bug fixes after time expires but not new features, and JetBrains lists last-minute changes among the reasons demos stall.
- **why** The final stretch is not short of ideas, it is short of days, and every one of these is a plausible-sounding way to spend three of them and arrive at the table with the same demo and a riskier build.

### Build three artifacts that survive without you, and skip the slide deck
- **severity** medium · **effort** M · **category** presentation-artifacts
- **evidence** https://info.devpost.com/blog/6-tips-for-making-a-hackathon-demo-video ; https://guide.mlh.com/general-information/judging-and-submissions/judging-plan (science-fair format, at-table) ; https://www.nicksingh.com/posts/win-hackathons-a-how-to-guide ; A:/unisubmit-main/unisubmit-main/screenshots/ (11 PNGs, all predate the branding engine, all show the old dark design system)
- **detail** MLH's recommended format is science fair — judges circulate to your table, there is no projector and no stage. A slide deck is the wrong artifact for that room. But Nick Singh's guide makes the complementary point: you cannot reliably identify who influences the final decision, and judges who were never assigned to you may take part in overall deliberation — so the artifacts that travel without you matter as much as the live pitch.

Devpost's video guidance: most hackathons cap at 3 minutes, **judges may not watch beyond it**, they review videos back to back, so front-load — start with a quick overview before going deeper, script it with seconds allocated per section, and do not speed up the audio to fit. Technical quality is called out separately: clear audio, YouTube rather than exotic hosts.

Your current artifact set has a specific problem: `screenshots/` contains 11 PNGs, and every one predates the branding engine — they show the old "Nocturne Laurel" dark system. They do not merely fail to help; they **contradict** the pitch. A judge comparing the screenshots to the demo sees two different products.
- **do** Build exactly three, in this order. **(1) A ≤2:00 screen-recorded demo video.** Same ordering as the live demo: money shot at 0:00, problem at 0:20, one flow to 1:20, measured number to 1:45. Write the script with per-section second counts before recording. Record with a headset mic, not laptop audio; upload to YouTube marked not-for-kids. Budget 2–3 hours including upload — Devpost's first tip is that teams underestimate exactly this. This doubles as the network-failure fallback ROADMAP-V4 §7 already calls for. **(2) One "money shot" still** — the same-code/two-schools side-by-side, 1280×640 — reused as the Devpost thumbnail, the README hero, and the first frame of the video. One image doing three jobs. **(3) Re-shoot `screenshots/`** on the branded demo instance; delete any that cannot be re-shot rather than shipping stale ones. 

**Skip the pitch deck** for science-fair judging. If the event has a separate stage round, cap it at three slides — money shot, the measured number, one limitation — and never read from them.
- **why** The video and the still image are what get looked at during deliberation, in a sponsor's follow-up, and by anyone who was not at your table. Stale screenshots that show a single dark theme actively undercut the one claim you most need to land.

### Rehearse the four questions that will be asked, including the two whose honest answers are currently strengths you are not using
- **severity** medium · **effort** XS · **category** qa-preparation
- **evidence** https://guide.mlh.com/general-information/judging-and-submissions/judging-plan (1 minute of Q&A per team) ; https://info.devpost.com/blog/hackathon-judging-tips (Bajza-Terlouw: polished UI with no code depth is a losing pattern) ; https://www.nicksingh.com/posts/win-hackathons-a-how-to-guide (prepare Q&A responses in advance) ; A:/unisubmit-main/unisubmit-main/README.md:46-49 ; CODEBASE-MAP.md:225-229
- **detail** MLH allocates a full minute to questions — a quarter of your total contact time, and the only part you cannot script the content of. Devpost's judges use Q&A specifically to test whether there is depth behind the interface. Four questions are near-certain here, and two of them have answers that are currently strengths you are not deploying:

**"Did you build this during the hackathon?"** — see the eligibility finding. Have the boundary stated in one sentence before you are asked.

**"How is this different from Moodle or Google Classroom?"** — the roadmap's own competitive research (notes/RESEARCH-NOTES.md) shows those platforms beat you on rubrics, late policy and bulk operations. Do not fight there. The honest answer is the deployment shape: one school, one container, 512MB, brandable in 90 seconds, works when the AI key is absent and when the network is bad.

**"What happens without an OpenAI key?"** — this is asked as a gotcha and is actually your best answer. The pipeline degrades to TF keywords plus an extractive summary, marks the insight `DEGRADED`, and **fabricates no tags**. "It tells you it is degraded rather than inventing data" is a strong, quotable line and it maps directly onto CGU-style rubric weight on transparency and explainability. It is currently buried at README.md:46-49.

**"Show me something that isn't finished."** — a judge clicking around will hit the empty room or a blank table. Answer it before they find it.
- **do** Write four answers, one sentence each, and say them out loud until they are automatic. Then prepare exactly three fallback artifacts to reach for when a judge digs: the evaluation table (precision@5/MRR by weight config), the contrast-pass table, and `CODEBASE-MAP.md` on screen for the judge who asks about architecture — it is a genuinely unusual artifact for a student project and answering "here is the one-file map I maintain so I can work on this without re-reading 17k lines" lands as engineering maturity in a way a code tour does not. Have the two security chains and the 127 tests ready *only* as answers to "what's under the hood" — never as volunteered content. Finally, rehearse the limitation you will volunteer unprompted: pick one real one (uploads are ephemeral without a mounted volume; sessions are in-memory so a redeploy logs everyone out) and say it before you are asked. Judges consistently reward candour and penalise overclaiming.
- **why** A minute is long enough to undo a good demo. These four questions are predictable, and two of them convert from liabilities into scored points purely by being rehearsed rather than improvised.

### Admin console: 67 of 105 <label> elements have no `for` — labels are visual only, not programmatic
- **severity** high · **effort** M · **category** accessibility
- **evidence** src/main/resources/templates/admin/accounts.html:139-140, :151-153, :159-161, :169-176; admin/units.html:21-38; admin/assignments.html:21-51; admin/branding.html:131-134
- **detail** The measured "72 unlabelled inputs" is one copy-pasted pattern, and it is almost entirely confined to /admin. Counting across all 39 templates: 105 <label> tags, only 38 carry a `for`. The 67 orphans break down as accounts.html 22, units.html 8, assignments.html 8, programmes/departments/branding 6 each, faculties/curricula 4 each, project-detail 2, groups 1 — 64 of 67 are under admin/. The pattern is always `<label class="form-label">Full name <span class="text-danger">*</span></label>` followed by an input with no `id` (accounts.html:139-140). Some inputs even already have ids (`createEmail` at :146, `createStudentId` at :171) and the label above still omits `for`. My script counts 73 controls (input/select/textarea, excluding hidden/submit) with no label association, no aria-label and no wrapping label. Meanwhile student/new-submission.html:18/43/49/57, register.html:25/32/38/46 and lecturer/review-split.html:132/144 do it correctly — so this is not a knowledge gap, it is one bad template that got cloned nine times. Second-order cost: a detached label is not a click target, so on a phone every admin field loses its label-sized tap area and only the input itself is tappable. Also note the required-field convention is a bare red `*` inside a span with no "* = required" legend anywhere — a screen reader reads "Full name star".
- **do** Mechanical fix, one pass over 10 admin templates: give every control an `id` and every label the matching `for`. Because it is the same shape everywhere, a find/replace over `<label class="form-label">X</label>` → `<label class="form-label" for="fooBar">X</label>` plus `id="fooBar"` on the next control is safe. While there, replace the bare `*` with `<span class="text-danger" aria-hidden="true">*</span>` on the label and add `required` + `aria-required` to the control, and add one "Fields marked * are required" line per form.
- **why** The admin console is exactly the surface a registrar or department clerk uses for 20-field data entry all day. With no programmatic labels a screen-reader user hears "edit text, blank" twenty times and cannot fill the form at all; a magnifier user loses the label/field pairing. It is also the cheapest large win available — one afternoon removes ~70 WCAG 1.3.1/4.1.2 failures.

### 12 modals, zero dialog semantics, zero focus management — and the open/close helper is copy-pasted verbatim into 6 files
- **severity** high · **effort** S · **category** accessibility
- **evidence** src/main/resources/templates/admin/accounts.html:130-134 and :360-370; admin/assignments.html:133-137, :202-204; admin/departments.html:64-68, :126-128; admin/faculties.html:61-65, :111-113; admin/programmes.html:64-68, :126-128; admin/units.html:112-116, :150-152; fragments/components.html:57-122
- **detail** Every admin CRUD page has 1-4 modals, all identical: `<div id="..." class="modal-overlay" style="display:none;" onclick="closeOnOverlay(...)">`. None has `role="dialog"`, `aria-modal="true"` or `aria-labelledby` pointing at its own <h2>. The identical three-function helper `openModal / closeModal / closeOnOverlay` (just `style.display='flex'` + `body.style.overflow='hidden'`) is duplicated character-for-character at accounts.html:360-370, assignments.html:202-204, departments.html:126-128, faculties.html:111-113, programmes.html:126-128 and units.html:150-152 — six copies, and app.js has no modal code at all. Consequences: (a) focus is never moved into the dialog, so a keyboard user presses "+ Create account" and focus stays on the trigger behind the overlay; (b) Tab walks straight through the invisible page behind it — no focus trap; (c) focus is never restored on close; (d) no Escape handler anywhere in the six admin files (only fragments/components.html:119-121 has one, for the announcement pop-up); (e) the close control is `<button class="btn btn-sm btn-secondary">✕</button>` — accessible name is the literal ✕ character, announced as "multiplication x" or nothing. The announcement pop-up in components.html:57-95 has the same problems plus it auto-opens on the student dashboard, so a screen-reader user gets a silently-appearing overlay they cannot find.
- **do** Delete all six copies and put one helper in app.js: on open, add `role="dialog" aria-modal="true" aria-labelledby=<the h2 id>`, store `document.activeElement`, focus the dialog's first control, trap Tab within `.modal-card`, close on Escape, and restore focus on close. Replace every `✕` button with `<button class="..." aria-label="Close dialog">` (keep the glyph as `aria-hidden`). Optionally switch to native `<dialog>.showModal()`, which gives the trap, Escape and inertness for free — it is well supported and would delete most of the helper.
- **why** Modals are the only way to create or edit anything in the admin console (accounts, faculties, departments, programmes, units, lecturer mappings). Right now none of them is operable by keyboard or screen reader, which means the entire administrative half of the product is inaccessible. Fixing it once in app.js also removes six divergent copies of the same code.

### "Request changes" writes REJECTED, and the same state is called four different things across two pages
- **severity** high · **effort** S · **category** usability
- **evidence** src/main/resources/templates/lecturer/review-split.html:122-124; src/main/java/com/unisubmit/domain/SubmissionStatus.java:3-15; src/main/resources/templates/student/dashboard.html:61, :117; fragments/components.html:27-30
- **detail** The lecturer's middle review button reads "Request changes" but carries `data-review-action="REJECTED"` (review-split.html:122-124). `SubmissionStatus` has no CHANGES_REQUESTED value — the enum is DRAFT, SUBMITTED, APPROVED, REJECTED, PROPOSAL, UNDER_REVIEW, FINAL, ARCHIVED (grep for CHANGES_REQUESTED across src/main/java returns zero hits, even though CODEBASE-MAP.md §6 lists it). So the student's `statusBadge` fragment renders `#strings.replace(status,'_',' ')` = the word **REJECTED** in a red `badge-rejected`. On one single page — student/dashboard.html — the identical state is labelled three more ways: the filter button says "Changes requested" (:61, `data-submission-filter="rejected"`), the rail card says "project needs changes" (:117), and the row badge next to them says REJECTED. A final-year student who is told to iterate reads "REJECTED" on their project, and the lecturer who clicked a button labelled "Request changes" has no idea that is what was sent.
- **do** Cheapest correct fix: add `CHANGES_REQUESTED` to SubmissionStatus, point the button at it, add `.badge-changes_requested` mapped to the amber tint (it is a "needs work" state, not a terminal one), and keep REJECTED for genuine rejection. If touching the enum is out of scope, at minimum stop rendering the raw enum name — give `statusBadge` a display-label map so REJECTED renders as "Changes requested" in amber, matching the filter and rail wording that already exist.
- **why** This is the single highest-emotion string in the product and it is wrong. It also makes the dashboard filter look broken: a student clicks "Changes requested" and gets back a list of things labelled REJECTED. Domain vocabulary that disagrees with itself on the same screen is the fastest way to lose a marker's trust in a final-year demo.

### Buttons delete their focus outline and replace it with a ~1.9:1 glow, failing WCAG 2.2 SC 1.4.11
- **severity** high · **effort** XS · **category** accessibility
- **evidence** src/main/resources/static/css/base.css:516; overridden global rule at base.css:2345-2346
- **detail** base.css:2346 defines a correct baseline — `:focus-visible { outline: 2px solid var(--primary); outline-offset: 2px; }` — and base.css:516 then throws it away for the app's most common control: `.btn:focus-visible { outline: none; box-shadow: 0 0 0 3px rgba(95, 191, 171, 0.32); }`. That glow composites over --canvas #121417 to roughly #2B4A46, giving 1.94:1 against the page and 1.85:1 against --surface #1a1d21 — SC 1.4.11 requires 3:1. Buttons make it worse than fields do: `.btn` has `border: 1px solid transparent` (base.css:498) and `.btn-secondary` keeps `--border-strong` on focus, so nothing else about the button changes — the sub-2:1 glow is the *only* focus signal. Form fields are fine by comparison (`.form-control:focus` at base.css:600-604 also uses `outline:none` and an even weaker 0.18-alpha glow, but it simultaneously swaps the border to --primary, ~5.4:1 against the resting border, which carries the state). The comment at base.css:2344-2345 says "More specific rules (.btn:focus-visible, .form-control:focus) still win" — the override is deliberate, the contrast just was not measured.
- **do** One line: change base.css:516 to `.btn:focus-visible { outline: 2px solid var(--primary); outline-offset: 2px; box-shadow: var(--shadow-soft); }` — or keep the glow aesthetic but make it opaque (`0 0 0 3px var(--primary)` reads ~8:1 and still looks like a ring). Then re-check `.dropzone:focus-within` (base.css:2418), which already does the right thing, for consistency.
- **why** Buttons are how you approve a submission, delete an account, apply a bulk import and sign out. A keyboard-only user currently cannot reliably see where they are on any page, and the app is one line away from passing. The reduced-motion and 44px-tap-target work elsewhere in this file shows the author cares about this — the focus ring is the gap.

### Group member picker is a custom combobox with no ARIA, no arrow keys, and Enter silently adds whoever is first
- **severity** high · **effort** M · **category** accessibility
- **evidence** src/main/resources/templates/student/groups.html:68-79 (markup), :144-216 (search), :225-233 (Enter handler), :190-209 (instant submit)
- **detail** The "Add member" control is a hand-rolled autocomplete against /api/users/search. Its problems compound: (1) the label at :68 has no `for` and the input at :70 has no `id`; (2) the input has no `role="combobox"`, no `aria-expanded`, no `aria-controls`, no `aria-autocomplete`; (3) the results container at :77 is a bare div with no `role="listbox"` and its children are plain buttons with no `role="option"`; (4) nothing announces "5 results" or "No matching students found" (:156-159) — there is no live region, so a screen-reader user types and hears absolute silence; (5) the only keyboard handling is `keydown` → Enter → `firstItem.click()` (:226-232), so ArrowDown does nothing and there is no way to reach the second result except Tab, and Escape does not close the list; (6) most seriously, that Enter path adds a real person to a real group instantly — :190-209 builds a form and calls `addForm.submit()` with no confirmation step. The search is debounced 200ms (:216), so a fast typist who hits Enter is acting on stale results and adds the wrong student. There is no undo available to them: removal is leader-only.
- **do** Either adopt the standard combobox pattern (`role="combobox" aria-expanded aria-controls aria-activedescendant` on the input, `role="listbox"`/`role="option"` on the list, ArrowUp/Down/Home/End/Escape, and an `aria-live="polite"` node saying "N students found"), or drop the custom widget entirely and use a native `<datalist>` plus an explicit "Add" button — the second option is a net code deletion and fits the project's minimal-JS ethos. Either way, remove the Enter-adds-first-match shortcut, or make Enter merely *select* the highlighted option and require a separate confirm.
- **why** Groups are the collaboration feature the whole product is pitched on, and this is its only entry point. Today a blind student cannot add a teammate at all, and a sighted one can add a stranger to their project group with a single mistimed Enter.

### Destructive actions are guarded inconsistently: removing a person has no confirmation and a ~12px hit area, while deleting a tag has a full cascade warning
- **severity** high · **effort** S · **category** usability
- **evidence** student/groups.html:56-61 and :41-45; static/css/base.css:1617-1626 vs :2115-2124; lecturer/review-split.html:118-128 and :156-159; admin/assignments.html:112 and admin/curricula.html:81 vs admin/units.html:91
- **detail** There are three separate inconsistencies here. (a) **No confirmation on the human-facing destructive actions.** Removing a member from a group (groups.html:56-61) posts immediately — no confirm, no undo, and the removed student is not the one clicking. "Leave group" (groups.html:41-45) is equally bare and equally unrecoverable (the leader must re-add you; there is no transfer). Approving a submission is explicitly terminal — review-split.html:156-159 renders "Approved and locked / No further review action is available" — yet the Approve button at :125-127 leads to a plain "Submit review" with no confirmation at all. (b) **The remove-member button is far below any tap-target floor.** `.remove-member-btn` is `font-size: 0.75rem; padding: 0 0.15rem; line-height: 1` (base.css:1617-1626) — roughly 12×12px. The 44px block at base.css:2115-2124 lists `.btn`, `.filter-btn`, `.header-link`, `.menu-toggle`, `.ai-tab-btn`, `.explore-tab`, `.form-control`, `.admin-sidebar a`, `.search-item`, `.nav-link` — `.remove-member-btn` is not among them, so the app's smallest control is also its least reversible one, and it fails even WCAG 2.5.8's 24px minimum. Its accessible name is the literal "✕" (groups.html:60). (c) **The 12 window.confirm strings disagree with each other.** Four explain the cascade well ("Delete this faculty? All departments inside will also be removed." faculties.html:42; units.html:91; departments.html:45; tags.html:75), two are a bare "Are you sure?" (assignments.html:112, curricula.html:81), and the account delete (accounts.html:111) never says what happens to that student's submissions.
- **do** Ranked by value: (1) add a confirm to remove-member and leave-group naming the person/group; (2) give `.remove-member-btn` `min-height/min-width: 44px` inside the ≤860px block, or fold it into that selector list; (3) add a confirmation step to Approve that states it is final — a checkbox in the form is enough and avoids another window.confirm; (4) rewrite the two "Are you sure?" strings to name the object and its cascade like the four good ones already do. If you later replace window.confirm with a real dialog, do it after the modal helper from finding #2 exists so there is one component, not thirteen.
- **why** The pattern to fix is that irreversibility and friction are inversely correlated here: deleting a *tag* is well guarded, kicking a *classmate* out of their project group is a single 12-pixel tap with no warning. On a phone that button sits inside a wrapping member chip, so a mis-tap is likely rather than theoretical.

### Admin tag rename/merge runs through window.prompt(), asking the admin to read numeric IDs out of a native dialog and type one back
- **severity** high · **effort** M · **category** usability
- **evidence** src/main/resources/templates/admin/tags.html:149-157 (rename), :159-198 (merge), :170/:184/:189 (alert)
- **detail** Two of the three tag operations have no UI at all. Rename (tags.html:150) is `prompt("Rename tag 'X' to:", currentName)`. Merge (tags.html:174-186) is worse: it builds a plain-text string `"[12] Machine Learning\n[13] Deep Learning\n..."`, shows it in a native `prompt()`, and asks the admin to **read the bracketed database ID and type the integer back**. It then hand-parses with `parseInt`, and reports every failure through `alert()` — "No other tags in this category to merge into." (:170), "Invalid target tag ID." (:184), "Could not find a tag with ID N in this category." (:189). With a realistic tag list this prompt is an unscrollable wall of text in a fixed-size OS dialog. The operation is also genuinely destructive — the confirm at :191 correctly says it "will update all student submissions referencing 'X' and delete it" — so the riskiest tag action is the one driven by typing a raw primary key. This page also carries the h1→h3 heading skip (:8 then :19) and its create-tag input at :40 has no label.
- **do** Both actions already have hidden POST forms sitting right there (`#jsRenameForm` :125-130, `#jsMergeForm` :132-137) — only the collection step is wrong. Replace rename with an inline `contenteditable`/text input on the tag row, and replace merge with a `<select>` of the sibling tags (the arrays are already inlined into the page at :141-147) inside a small dialog reusing the shared modal helper. Route the three `alert()` calls through the existing flash-alert fragment. That deletes ~40 lines of prompt-parsing.
- **why** Knowledge tags feed the recommendation engine — CODEBASE-MAP §5 says Technology and ResearchArea are the two signals that matter — so tag hygiene is the highest-leverage admin task in the product, and it is the one with the worst interface. window.prompt is also unstyled, unbranded, blocks the page, and is suppressed outright by some mobile browsers, so on a phone the admin may simply be unable to merge tags.

### A failed submission redirect throws away the whole form including the uploaded file — on the app's slowest, most expensive action
- **severity** high · **effort** S · **category** usability
- **evidence** src/main/java/com/unisubmit/controller/StudentController.java:112-134; src/main/resources/templates/student/new-submission.html:44-72; contrast register.html:27, :34, :40
- **detail** `createSubmission` is post-redirect-get on every failure path. Both the missing-unit branch (:120-123) and the catch-all (:126-129) return `"redirect:/student/submission/new" + (groupId != null ? "?groupId=" + groupId : "")` — only `groupId` survives. `title`, `helpWanted` and `unitId` are dropped, and new-submission.html:44 and :50 have no `th:value`, so the fields render empty. The 25MB multipart file is gone entirely and must be re-picked and re-uploaded. The failure modes this hits are the common ones, not exotic: past the deadline with no late window, a student whose programme has no matching curriculum (SubmissionService raises a deliberate message for exactly this), or a file over the cap. register.html shows the team already knows the fix — it repopulates `formName`, `formStudentId`, `formPhone` and pushes `data-selected` into the cascading selects at :48/:57/:64. Separately, :127 pipes the raw `e.getMessage()` of *any* exception straight to the student's flash banner.
- **do** Flash `title`, `helpWanted` and `unitId` back as attributes and bind them with `th:value` / `th:selected` in new-submission.html, mirroring register.html. Move the checks that can be made before reading the file (unit selected, deadline open, curriculum resolvable) ahead of the multipart read so the student is stopped before uploading rather than after. Replace the bare `catch (Exception e)` message pass-through with a typed catch for the known domain exceptions plus a generic fallback string.
- **why** Built for students in Kenya on mobile data, this is the worst possible place to lose state: a 20MB re-upload over a metered 3G connection, on the deadline night when the deadline-guard errors are most likely to fire. Every other multi-field form in the app already survives a round trip — this one, the app's core action, does not.

### Every "which one is selected" state in the app is CSS-only — and one tablist is declared with no tabs inside it
- **severity** medium · **effort** S · **category** accessibility
- **evidence** grep aria-current across templates/ = 0 hits; fragments/components.html:234-240 and :242/:248/:254; static/js/app.js:13-21; explore.html:24-41; student/dashboard.html:54-62
- **detail** Not one `aria-current` exists in 39 templates. `markActiveNav` (app.js:13-21) adds only a `.active` class, styled at base.css:252-253 as a colour and weight change — so neither the top nav nor the bottom tab bar tells assistive tech which page you are on. The same gap repeats for `.filter-btn.active` (dashboard.html:54-62, base.css:1075) and `.explore-tab-active` (explore.html:25/:34, base.css:1457): no `aria-current`, no `aria-pressed`. The worst instance is the AI panel: components.html:234 declares `<div class="ai-tabs" role="tablist">` but its children (:235-239) are plain `<button type="button" data-ai-tab="...">` with no `role="tab"`, no `aria-selected`, no `aria-controls`, and the three panels at :242/:248/:254 have no `role="tabpanel"`. A declared-but-empty tablist is actively worse than no ARIA — a screen reader announces "tab list" and then finds nothing tabbable in it. Credit where due: the lecturer review action group is the one place done right — app.js:100-105 sets `aria-pressed` on all three buttons and the wrapper has `role="group" aria-label="Choose review outcome"` (review-split.html:118). That is the pattern to copy.
- **do** Three small edits: (1) in `markActiveNav`, also set `aria-current="page"` alongside the class; (2) on `.filter-btn` and `.explore-tab`, add `aria-pressed` / `aria-current="true"` in the same place the active class is applied; (3) in components.html either complete the tab pattern (`role="tab"` + `aria-selected` + `aria-controls` on the buttons, `role="tabpanel"` + `aria-labelledby` on the panels, with app.js:180-193 toggling `aria-selected`) or simply delete `role="tablist"` at :234 and let them be plain buttons — the honest option, and a one-character-class fix.
- **why** These are the app's wayfinding signals. Without them a screen-reader user has no idea which of eight nav links is the current page, which of nine dashboard filters is applied, or which AI tab is showing — they just hear an undifferentiated list of buttons. The review-action group proves the fix takes about three lines per site.

### The four auth pages hand-roll their alerts without role="alert", then autofocus past them — and mobile deletes them after 4s
- **severity** medium · **effort** XS · **category** accessibility
- **evidence** login.html:21-43 and :50; register.html:18 and :27; reset-password.html:22-25 and :32; forgot-password.html:28; contrast fragments/alerts.html:11-28; static/js/app.js:405-417
- **detail** fragments/alerts.html exists precisely for this and gets it right — `role="status"` on the three success variants (:11/:14/:17), `role="alert"` on the three error variants (:20/:23/:26) — and its own header comment says "pages never hand-roll alerts". All four unauthenticated pages hand-roll anyway and none of them includes the fragment. login.html:21-43 alone has six banners with no role: `?error` ("Wrong ID or password"), `?suspended` ("Account suspended" + reason), `?locked` ("Temporarily locked" + the 15-minute lockout message), plus logout/registered/reset. Then login.html:50 puts `autofocus` on the username field — below the banner — so a screen reader starts reading at the input and the user never learns *why* the page reloaded. Same shape at register.html:18 + :27 and reset-password.html:22-25 + :32. On top of that, app.js:405-417 converts every `.alert` inside `#mainContent` to a toast at ≤860px and calls `alert.remove()` after 4000ms for anything that is not `.alert-danger` — with no live region, so on a phone a success message is announced to nobody and is gone from the DOM before a slow reader reaches it. admin/branding.html:21-22 hand-rolls the same way, and because admin/layout.html:98 already renders the shared fragment (which matches `successMessage` at alerts.html:14), a branding save shows the *same banner twice*.
- **do** Replace the hand-rolled blocks on all four auth pages and on branding.html with `<div th:replace="~{fragments/alerts :: alerts}"></div>` — the fragment already handles `error`, `errorMessage`, `param.error`, `success`, `successMessage`, `param.success`, and the auth pages' extra `param.suspended`/`param.locked`/`param.locked` cases can be added to it in six lines. Drop `autofocus` where an error banner may be present, or move focus to the banner instead. In initMobileToasts, wrap toasts in a live region and stop auto-removing anything the user has not acknowledged.
- **why** These are the highest-stakes messages the app sends — "your account is suspended", "you are locked out for 15 minutes", "wrong password". They are currently the only messages in the product with no ARIA role, and on a phone the non-error ones self-destruct after four seconds. Fixing it is deleting code, not writing it: the correct component already exists and is used on 14 other pages.

### On phones .table-stack hides every <thead> with no per-cell labels, and the busiest table opts out of stacking entirely
- **severity** medium · **effort** M · **category** usability
- **evidence** static/css/base.css:2214-2247 (esp. :2222 `thead { display: none }`); admin/evaluation.html:106-124; admin/import.html:128-146 and :81-99; admin/accounts.html:31; no `scope=` or `<caption>` in any of 16 tables
- **detail** `.table-stack` (base.css:2214-2247) reflows table rows into flex cards at ≤640px and sets `.table-stack thead { display: none }` at :2222 without substituting `td::before { content: attr(data-label) }`. For self-describing rows this is fine — lecturer/dashboard.html:68-106 reads "Title / Jane Doe / 12 Mar 2026 / UNDER REVIEW" and survives. For numeric rows it destroys the data. admin/evaluation.html:106-124 collapses "Configuration / Weights / Retrieved / Precision@5 / MRR" into a name followed by four bare unlabelled numbers — the entire point of the ablation table is comparing those columns. admin/import.html:128-146 turns "# / Name / Phone / Admission no. / Programme / Result" into a name plus four undistinguished strings, and :81-99 does the same for lecturers, which is exactly where an admin must spot which column is malformed before applying the import. Inversely, admin/accounts.html:31 is the one table that uses plain `.table-wrap` **without** `.table-stack` — so the app's largest and most-visited table (5 columns, and an actions cell holding Edit / Reset password / Make class rep / Delete) is the only one that side-scrolls on a phone, pushing all four action buttons off-screen. Across all 16 tables there is not a single `scope="col"` on a `<th>` and not a single `<caption>`.
- **do** Add `th:data-label` to the `<td>`s in the numeric tables and one CSS rule inside the ≤640px block: `.table-stack tbody td[data-label]::before { content: attr(data-label) ": "; color: var(--text-muted); }` — the existing `.stack-full` / `td:last-child` layout still works. Add `.table-stack` to admin/accounts.html:31 (the row already carries `data-*` attributes that the stacked layout does not disturb), or give its actions cell a wrapping overflow menu. Add `scope="col"` to the `<th>`s and a `<caption class="sr-only">` per table.
- **why** CODEBASE-MAP calls the mobile shell primary (PWA, TWA/APK, bottom nav, 44px targets) — so the phone view of a table is the real view, not a fallback. An admin validating a 200-row student import on a phone currently cannot tell an admission number from a phone number, and the accounts table — the one they will open most — is the single table that makes them scroll sideways to reach Delete.

### admin/branding.html is built outside the design system: 63 inline styles, 4 classes that do not exist in base.css, and a keyboard-unreachable dropzone
- **severity** medium · **effort** M · **category** usability
- **evidence** admin/branding.html:34-39 (dropzone), :11 (.head-actions), :30/:169 (.card-body), :155 (.btn-primary), :167 + :310/:313 (.badge-success), :21-22 (duplicate alert); contrast student/new-submission.html:19-27
- **detail** Counting `style="` per template: branding.html has 63, the next highest is fragments/components.html with 10 and most pages have 0-2. Four of its classes have no rule anywhere in base.css — I grepped each: `.head-actions` (:11; the real class is `.page-head-actions`, base.css:414, so the Reset button loses its flex layout), `.card-body` (:30, :169 — no padding), `.btn-primary` (:155), and `.badge-success` (:167). That last one matters: the contrast checker sets `contrastBadge.className = 'badge badge-success'` on pass (:310) and `'badge badge-rejected'` on fail (:313) — so the **fail** state is correctly red and the **pass** state renders as an unstyled neutral badge. Worse, the static markup at :167 ships `<span id="contrastBadge" class="badge badge-success">WCAG AA Compliant</span>`, so an admin who never uploads a logo sees an unconditional compliance claim that no check has produced. The logo uploader at :34-39 is a plain `<div id="dropzone" style="cursor:pointer">` wrapping `<input type="file" style="display:none">` — no `tabindex`, no `role`, no `<label for>` (grep for tabindex across all templates returns zero hits), so it cannot be focused or activated by keyboard at all; student/new-submission.html:19-27 solves the identical problem correctly with `<label class="dropzone" for="file">`. Finally :21-22 hand-rolls the flash banner that admin/layout.html:98 already renders via fragments/alerts.html:14, so every successful save shows the message twice. Label casing also diverges — "Upload Logo", "Primary Brand Hue", "Save Theme Configuration" against the app's sentence case ("Full name", "Project title", "Create submission").
- **do** Swap `<div id="dropzone">` for the `<label class="dropzone" for="logoFileInput">` pattern already proven in new-submission.html (drop the `display:none` on the input and let `.dropzone-input` style it). Fix the four class names — `.head-actions`→`.page-head-actions`, delete `.card-body`/`.btn-primary`, and either add a `.badge-success` alias to base.css:723 next to `.badge-approved` or change both :167 and :310 to `badge-approved`. Make the initial badge read "Contrast not checked" until the JS runs. Delete :21-22. Then migrate the 63 inline styles into named classes and re-case the labels — worth doing as a follow-up pass rather than blocking on it.
- **why** Branding is the first screen a pilot university's IT admin will open, and it is the page that visually announces whether this is a product or a project. Right now it is the one page whose CSS classes are guesses, whose logo uploader excludes keyboard users, whose success banner appears twice, and which claims WCAG AA compliance before measuring anything. It also mutates `:root` tokens app-wide (layout.html:11 injects `themeCssBlock`), so a broken contrast indicator here degrades every other screen.

### Class-rep powers are reachable only by a mobile client that does not exist
- **severity** critical · **effort** L · **category** orphan-feature
- **evidence** src/main/java/com/unisubmit/controller/api/ClassRepApiController.java:29-30; src/main/java/com/unisubmit/controller/admin/AdminAccountController.java:88-99; src/main/resources/templates/admin/accounts.html:77,101-106; ROADMAP-V4.md:259
- **detail** Admin › Accounts renders a working 'Make class rep' button and a 'Class rep' badge (accounts.html:101-106, :77). Promoting a student writes StudentProfile.classRep and CustomUserDetailsService grants a bare CLASS_REP authority (CustomUserDetails.java:52-53). But every action that authority unlocks — list my units, list classmates, enrol a classmate, assign a lecturer to a unit, post a class notice — lives exclusively on ClassRepApiController, mapped to /api/v1/student/class-rep/** and therefore served only by the stateless JWT chain (ApiSecurityConfig @Order(1), securityMatcher "/api/v1/**"). Grepping every template, app.js and fragment for 'class-rep', 'classRep' or 'CLASS_REP' returns exactly one hit outside the admin toggle — and that hit is the toggle itself. ROADMAP-V4.md:259 confirms the client side was never built: 'The /api/v1 server side exists and is tested; the client does not.' So a promoted rep logs into the web app and sees a student dashboard identical to the one they had before. The admin has performed an action with no observable effect anywhere in the product.
- **do** Either (a) add a session-chain web surface for the three rep actions — a /student/class/** controller reusing ClassRepService verbatim, plus a 'My class' nav entry gated on sec:authorize="hasAuthority('CLASS_REP')" — or (b) hide the promote button and the badge behind the same flag that would ship the Flutter client. Shipping a permission grant with no consumer is worse than shipping neither. Option (a) is the higher-value one: enrolment-by-rep is exactly the workaround for the nine-step import problem already on the roadmap, because a rep can fix their own class roster without the admin re-running an import.
- **why** This is the single largest gap between what the admin console promises and what the product does. In a demo, an evaluator who clicks 'Make class rep' and then signs in as that student sees nothing — the feature reads as broken rather than unbuilt.

### Blind review is defeated by the queue the lecturer must pass through to reach it
- **severity** high · **effort** S · **category** correctness
- **evidence** src/main/resources/templates/lecturer/dashboard.html:92; src/main/java/com/unisubmit/controller/LecturerController.java:83-138 (no blindReview attribute) vs :153 and :179
- **detail** BLIND_REVIEW=true is honoured in exactly two places: the review page (LecturerController:153 sets the model attribute; review-split.html:12 and :169 render 'Anonymous student') and the marks CSV (LecturerController:179). The dashboard handler never computes blindReviewMode at all, and lecturer/dashboard.html:92 prints th:text="${sub.student.name}" unconditionally in the queue table. The only navigation path to a review is that table — its rows are the links (dashboard.html:80-85). A lecturer therefore reads every student's real name in the queue, then clicks through to a page that ceremoniously tells them identity is hidden, complete with a persistent banner (review-split.html:23-30) and the hint 'Submitting a grade reveals the student's identity' (:149). The masking is theatre; the identity was already disclosed one click earlier.
- **do** Compute the graded/ungraded state in LecturerController.dashboard the same way the detail handler does, pass a set of submission ids whose identity may be shown, and mask dashboard.html:92 (plus the group-name column at :95-96, which also identifies the team). Cheapest correct version: reuse the existing per-submission 'has any feedback with a grade' predicate in the batch that already runs for nearDuplicateIds (:127-128), so no extra query is added.
- **why** A blind-review feature that leaks on the index page is a correctness bug with academic-integrity consequences, and it is the kind of thing a project supervisor will test first.

### APPROVED is a terminal state with no exit for any role
- **severity** high · **effort** M · **category** dead-end-state
- **evidence** src/main/resources/templates/lecturer/review-split.html:108,156-159; src/main/resources/templates/student/submission-detail.html:42,60-62; src/main/java/com/unisubmit/service/SubmissionService.java:168-170; src/main/java/com/unisubmit/controller/admin/AdminDashboardController.java:56-63
- **detail** One misclick on 'Approve' (review-split.html:125-127) freezes a submission permanently. The review form is wrapped in th:if="${submission.status.name() != 'APPROVED'}" (:108), so the lecturer who approved by mistake is shown 'Approved and locked — No further review action is available for this submission' (:156-159) and no control to undo it. On the student side the upload section is gated identically (submission-detail.html:42) and replaced with 'Approved — uploads closed' (:60-62), and SubmissionService.addNewVersion throws IllegalStateException before writing (:168-170). The admin console has no submission management page at all — AdminDashboardController touches SubmissionRepository only for countByStatus (:56-63), and no admin controller ever loads or mutates a Submission. Nothing in the codebase deletes a submission except a demo seeder (CollaborationDemoSeeder.java:489). Note that addFeedbackAndReview itself has no APPROVED guard (:392-394): the handler would happily accept the reversal, only the UI refuses to offer it.
- **do** Since the service already permits it, the minimal fix is to keep rendering the review form when status == APPROVED but collapse it behind a secondary 'Reopen for revision' control that posts status=UNDER_REVIEW, writing an AuditAction.STATUS_CHANGED row (the audit trail already records this at :396-397). This is also the roadmap's own 3.2 principle — prefer undo over confirm — applied to the single most consequential irreversible action in the product.
- **why** Approval is the one action in the whole app that cannot be taken back, and it is a single unguarded click next to 'Request changes'. On a shared demo instance one wrong click permanently bricks a submission for the student who owns it.

### An AI insight stuck in PENDING or PROCESSING has no recovery path and no reaper
- **severity** high · **effort** M · **category** dead-end-state
- **evidence** src/main/resources/templates/fragments/components.html:199-225; src/main/resources/templates/student/submission-detail.html:74-80; src/main/java/com/unisubmit/service/AIInsightService.java:110-112,131-133; src/main/resources/static/js/app.js:33-53; src/main/java/com/unisubmit/config/DeadlineReminderScheduler.java:25
- **detail** The PENDING and PROCESSING branches of aiInsightPanel render a spinner and skeletons with no button of any kind (components.html:199-225) — the Retry button exists only in the DEGRADED (:232) and FAILED (:282-285) branches. The student's 'Re-run AI analysis' form is gated on status being COMPLETED or DEGRADED (submission-detail.html:74). Both server entry points refuse in-flight rows by construction: retryAnalysis claims only FAILED/DEGRADED (AIInsightService:110-112) and rerunAnalysis only COMPLETED/FAILED/DEGRADED (:131-133) — PENDING and PROCESSING are deliberately excluded to make the transition atomic. The client polls 40 times at 3s and then silently gives up; app.js:37-39 even names the scenario in a comment: 'a stuck-PENDING insight'. Nothing ever moves such a row: the only @Scheduled bean in the entire tree is DeadlineReminderScheduler (:25). Analysis is an in-process async pipeline, so any crash, OOM in the 512MB container, or redeploy mid-run leaves the row at PROCESSING forever. The blast radius is wider than the spinner: AIInsightStatus.hasContent() gates matching and search, so that submission is also permanently invisible to recommendations, Explore search and collaboration discovery.
- **do** Add a stale-run sweep — a @Scheduled job that flips any insight in PENDING/PROCESSING whose updatedAt is older than a few minutes to FAILED with errorMessage 'Analysis was interrupted — retry.', which immediately re-enables the existing Retry button with zero new UI. As a belt-and-braces measure, render the Retry button in the PENDING/PROCESSING branches too once the panel has been polling past the 40-attempt bound, and widen retryAnalysis to accept PROCESSING rows older than the timeout.
- **why** This is silent and permanent: the student sees 'Analysing…' indefinitely, has no button, and their project quietly drops out of every discovery surface the app is built around. A single redeploy during a demo can produce it.

### Lecturer/admin self-service password reset dead-ends with a UI that claims a code was sent
- **severity** high · **effort** S · **category** broken-flow
- **evidence** src/main/java/com/unisubmit/service/EmailService.java:38,44-49; src/main/resources/application.yml:58; .env.example:32; src/main/java/com/unisubmit/controller/AuthController.java:78-81; src/main/resources/templates/reset-password.html:16-19,22-24
- **detail** EmailService gates every send on spring.mail.username being non-blank (:38); unconfigured, it logs the code and returns (:45-49). The default is unset in both application.yml (:58 username: ${MAIL_USERNAME:}) and .env.example (:32 MAIL_USERNAME=). AuthController nevertheless redirects unconditionally to /reset-password?sent (:80), and reset-password.html:22-24 then asserts 'If that email is on file, a 6-digit code was just sent — it expires in 15 minutes.' On a default deployment that statement is false: the code exists only in the server log. The page offers no alternative — its own copy (:16-19) sends students elsewhere, which means a lecturer who lands here has no path at all. The admin-mediated fallback in AuthController:83-90 is skipped for non-student roles, so no admin is even notified that the lecturer is locked out. Requesting again just re-lands on the same form, and inside the 60s cooldown PasswordResetService.requestCode returns without issuing anything (:70-74) while the UI still claims a code was sent.
- **do** Make the claim conditional on the capability: expose EmailService.isConfigured(), and when mail is off, fall through to the same admin-notification branch students use and redirect to /login?resetRequested. Keep the emailed-code path for configured deployments. A one-line startup WARN when MAIL_USERNAME is unset would also stop this being discovered by a locked-out user.
- **why** This is the only self-service recovery path for the two roles that can actually be locked out of a running deployment, and out of the box it fails while telling the user it succeeded — the worst possible failure mode for an auth flow.

### A submission can never be withdrawn, deleted or re-filed by anyone
- **severity** high · **effort** M · **category** missing-flow
- **evidence** src/main/java/com/unisubmit/controller/StudentController.java:112-162 (no delete mapping); src/main/java/com/unisubmit/service/SubmissionService.java (no delete method); src/main/java/com/unisubmit/controller/admin/AdminDashboardController.java:56-63
- **detail** SubmissionService exposes create, addNewVersion, access checks and addFeedbackAndReview — and nothing else. There is no delete, no withdraw, and no way to change a submission's unit or curriculum after creation (the curriculum is resolved once, at SubmissionService:92-110, and never revisited). The only submissionRepository.delete call in main/ is inside CollaborationDemoSeeder (:489). No admin controller loads a Submission — the admin console's entire relationship with submissions is eight countByStatus calls (AdminDashboardController:56-63). Combined with the strict curriculum rule, the consequences compound: a student who picks the wrong unit from the dropdown creates a permanent row that sits in the wrong lecturer's queue, counts toward the admin's dashboard totals, feeds the recommendation corpus, and can be neither moved nor removed by the student, the lecturer, or the administrator.
- **do** Add a student-side withdraw for submissions with no feedback yet (soft-delete via a status of ARCHIVED, which already exists in the enum and already has a badge style), plus an admin submissions list — even a read-only table with a single archive action — so the console has some handle on the entity the whole product is about. The list doubles as the missing 'submissions nobody is assigned to review' view.
- **why** Every wrong upload is permanent and pollutes the review queue, the dashboard counters and the matching corpus. A lecturer or admin will ask for this within an hour of real use.

### The new-submission unit picker offers units the student cannot submit to, and rejects them only after the upload
- **severity** medium · **effort** S · **category** broken-flow
- **evidence** src/main/java/com/unisubmit/controller/StudentController.java:96-103,120-129; src/main/java/com/unisubmit/service/SubmissionService.java:92-110; src/main/resources/templates/student/new-submission.html:58-63
- **detail** The dropdown is populated with every unit in the student's department (StudentController:99) — or, for a student with no programme on file, literally every unit in the institution (:102). But createSubmission requires a Curriculum row linking that unit to the student's own programme and otherwise throws 'Your programme isn't linked to this unit yet — ask your admin to add it under Curricula' (SubmissionService:100-106). The dropdown is therefore a superset of the valid choices, and the mismatch surfaces only after the student has selected a file, typed a title, and uploaded. Recovery is worse than the error: the catch at StudentController:126-129 redirects to /student/submission/new preserving only groupId, so the file attachment, the title, the helpWanted line and the chosen unit are all discarded, and the student must redo everything to hit the same wall. Separately, new-submission.html:58-63 has no empty-state branch — when units is empty the required select contains only its disabled placeholder, so the form can never be submitted and nothing on the page explains why.
- **do** Filter the dropdown to units that actually have a curriculum for the student's programme (curriculumRepository.findByProgrammeId already exists in the same shape used at SubmissionService:94), and render the remaining department units as a disabled optgroup labelled 'not yet linked to your programme' so the student can see what to ask the admin for. Add the empty state, and preserve title/unit/helpWanted across the error redirect the way AuthController.backToForm already does for registration (:177-187).
- **why** This is the first thing a student does in the product, it fails after the slowest step, and it discards their work on failure — the exact shape of the onboarding problem the roadmap is already targeting, but on the student side rather than the admin side.

### The collaboration flow neither announces itself nor leads anywhere after acceptance
- **severity** medium · **effort** S · **category** broken-flow
- **evidence** src/main/java/com/unisubmit/service/CollaborationRequestService.java:34-60,62-78 (zero notification calls in the file); src/main/resources/templates/fragments/navbar.html:71-76,108-111; src/main/resources/static/css/base.css:2373; src/main/resources/templates/student/inbox.html:78-96
- **detail** Two defects bracket the feature the product is built around. At the front: CollaborationRequestService contains no reference to NotificationService whatsoever — createRequest (:34-60) saves a row and stops, and acceptRequest/declineRequest (:62-83) likewise. Every other event in the app raises an AppNotification (SubmissionService does so for feedback and status change), so a collaboration request is the one thing that arrives with no bell entry. The only signal is the header badge at navbar.html:71-76 — and base.css:2373 hides that header link outright at ≤860px, the mobile shell that is the PWA/TWA target, while the bottom-nav 'Inbox' tab that replaces it (navbar.html:108-111) carries no count. On a phone there is no indication a request exists. At the back: acceptance produces a Collaboration row that renders as a list item with an 'active' badge, the partner's name, and a link to the project (inbox.html:78-96). There is no message thread, no contact detail, no way to add the partner to a project group, and no way to end the collaboration. Two students the LLM matched, wrote a joint pitch for, and told what each brings, cannot exchange a single word inside the app.
- **do** Raise an AppNotification on create/accept/decline — three calls, matching the existing NotificationType usage — and add the pending count to the bottom-nav Inbox tab so mobile has a signal. For the terminal state, the cheapest meaningful next step is a button that creates a ProjectGroup seeded with both students (ProjectGroupService.createGroup + addMember already exist), turning 'active collaboration' into the thing the students actually need.
- **why** Collaboration matching is the app's differentiator and the centrepiece of the demo. Today it silently drops requests into a page the recipient has no reason to open, and its success state is a badge that does nothing.

### Group leaders are trapped in their own groups, and the admin's group powers have no entry point
- **severity** medium · **effort** S · **category** dead-end-state
- **evidence** src/main/resources/templates/student/groups.html:40-45; src/main/java/com/unisubmit/controller/ProjectGroupController.java:47-49,73-84,86-88; src/main/java/com/unisubmit/security/SecurityConfig.java:57-58; src/main/resources/templates/admin/layout.html:35-95
- **detail** Two halves of the same gap. Leaving is offered only to non-leaders — groups.html:41 wraps the form in th:if="${group.leader.id != #authentication.principal.user.id}" and the comment concedes 'the leader cannot (no transfer yet)'. There is no leadership transfer, no group delete endpoint anywhere in ProjectGroupController, and no admin screen for groups. A student who mistypes a group name at groups.html:11-15 owns that group permanently; it keeps appearing in their own group picker on the new-submission form (new-submission.html:66-73) forever. Meanwhile the escape hatch exists but is unreachable: SecurityConfig:57-58 grants /groups/** to STUDENT and ADMIN, and both addMember (:48) and removeMember (:87) explicitly authorise hasRole('ADMIN'), yet the /groups link in navbar.html:22 is gated sec:authorize="hasRole('STUDENT')" and the admin sidebar (admin/layout.html:35-95) has no groups entry at all. The admin can only exercise these permissions by typing the URL. Lecturers, by contrast, are refused outright by SecurityConfig:58 — reasonable, but it means group composition is invisible to the person grading group work.
- **do** Add a leader-side 'Delete group' (only when the group owns no submissions) plus 'Transfer leadership' to groups.html, and add a Groups entry to the admin sidebar so the ADMIN branch of those two @PreAuthorize expressions is actually reachable. Both are small and turn dead permissions into a real recovery path.
- **why** A typo in a group name is unfixable by anyone through the UI, and the one role empowered to fix it has no link to the page where the power lives.

### 'Request changes' writes REJECTED, and the state the UI names does not exist
- **severity** medium · **effort** S · **category** correctness
- **evidence** src/main/resources/templates/lecturer/review-split.html:122-124; src/main/resources/templates/lecturer/dashboard.html:27-28; src/main/java/com/unisubmit/domain/SubmissionStatus.java:3-14; src/main/resources/templates/fragments/components.html:27-29; CODEBASE-MAP.md:178-179
- **detail** The review page's middle action is labelled 'Request changes' but carries data-review-action="REJECTED" (review-split.html:122-124), and app.js copies that value into the hidden status field submitted to the handler. The dashboard filter labelled 'Changes requested' likewise resolves to statusFilter='REJECTED' (dashboard.html:27-28). SubmissionStatus has eight constants and CHANGES_REQUESTED is not among them (SubmissionStatus.java:3-14); grepping the whole of src/main for CHANGES_REQUESTED returns nothing. So the state the interface names in two places has no representation in the model. The consequence lands on the student, because statusBadge simply prints the raw enum name (components.html:27-29): a lecturer clicks a button that says 'Request changes', and the student's dashboard shows a red badge reading REJECTED. The same wording reaches them again through the status-change notification, which interpolates the enum name directly (SubmissionService:401-403). Worth noting for the docs pass: CODEBASE-MAP.md:178-179 lists CHANGES_REQUESTED as a live status, so the map is describing a state the code never had.
- **do** Add CHANGES_REQUESTED to the enum, point the button and the filter at it, and give it a badge-changes_requested rule in base.css (the badge class is derived from the lowercased name, so no template change is needed beyond the two literals). Reserve REJECTED for actual rejection. Fix CODEBASE-MAP §6 in the same pass.
- **why** A lecturer asking for a revision tells the student their project was rejected. That is a bad moment for the student and a misrepresentation of the lecturer's intent, and it happens on the most-used control in the review flow.

### Re-downloading the import credentials CSV returns a bodyless 404 with no route back
- **severity** medium · **effort** S · **category** error-handling
- **evidence** src/main/java/com/unisubmit/controller/admin/AdminImportController.java:111-121,265-275; src/main/resources/templates/admin/import.html:17-22,43-48
- **detail** resultsCsv removes SESSION_CREDS before returning the file (:119), which is the intended one-shot behaviour. But the second request — a double-click, a browser that blocks the download, a refresh after the save dialog, an admin on a phone where the download does not surface — hits the guard at :115-117 and gets ResponseEntity.notFound().build(): HTTP 404 with an empty body, no themed error page, no message, no link. The same applies to the lecturer variant at :270-272. From there the results page is gone too, because /admin/import/students/results now renders creds == null and import.html falls through to the plain upload form (:161). The plaintext passwords for every account just created are unrecoverable; the only remedy is resetting each account individually at /admin/accounts/{id}/reset-password. Because sessions are in-memory (documented in CODEBASE-MAP §7), a redeploy between apply and download does the same thing.
- **do** Do not clear the session on download — clear it on an explicit 'I've saved these, discard them' action, or on a short TTL. If the guard must stay, redirect to /admin/import with a flash explaining the list was already downloaded and pointing at per-account reset, instead of returning an empty 404. The page copy at import.html:22 already warns the list disappears; the failure mode should match that warning rather than silently 404ing.
- **why** Bulk import is the primary onboarding path; a mis-timed second click on a 200-row import strands 200 accounts with unknown passwords and gives the admin a blank page as the only feedback.

### /discover is an orphan route that renders a contentless page for lecturers and admins
- **severity** low · **effort** XS · **category** orphan-route
- **evidence** src/main/java/com/unisubmit/controller/ExploreController.java:98-101; src/main/resources/templates/explore.html:48,86; src/main/resources/templates/admin/layout.html:23,105; src/main/java/com/unisubmit/controller/AuthController.java:198-201
- **detail** GET /discover is mapped for every authenticated role and redirects unconditionally to /explore?tab=discover (:98-101). No template links to it. For a non-student the destination has nothing to show: the search block is gated th:if="${activeTab == 'search'}" (explore.html:48) and so does not render, while the discover block is gated sec:authorize="hasRole('STUDENT')" (:86) and is suppressed. The lecturer or admin gets the hero and a lone 'Search' tab above an empty region — no results, no empty state, no explanation. The route is still advertised as a live feature in CODEBASE-MAP §4 and is prefetched by the admin layout's speculation rules (admin/layout.html:23). A second, smaller instance of the same pattern sits at admin/layout.html:105: the footer's 'Back to app' points at /, which AuthController.index (:198-201) redirects straight back to /admin/dashboard for an ADMIN — a link that always returns the user to the page they are already on.
- **do** Make discoverRedirect role-aware — send non-students to /explore?tab=search — or drop the mapping and the prefetch pattern entirely, since nothing links to it. For the footer, point 'Back to app' at /explore, which is the only shared surface an admin actually has.
- **why** Both are small, but they are the two navigation controls in the product that provably lead nowhere, and the /discover one produces a page with no content and no way to tell that it worked.

