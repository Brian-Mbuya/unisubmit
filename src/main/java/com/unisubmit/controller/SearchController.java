package com.unisubmit.controller;

/**
 * Phase 7 — /search is now handled by ExploreController as a redirect to /explore?tab=search.
 * This class is kept as a placeholder to avoid breaking any potential references.
 */
public class SearchController {
    // Intentionally empty — see ExploreController
}
